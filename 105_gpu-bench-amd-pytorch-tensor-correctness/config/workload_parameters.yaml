schema_version: 1.0.0
purpose: Canonical definitions for parameters shared by at least two workloads.
source_workbook: BenchmarkSpecDefinitions.xlsx::Workload_Definitions
source_of_truth: Workload-specific parameter placement and execution descriptions remain authoritative in the workbook; this
  catalog standardizes shared terminology and exposes semantic overrides.
generation_note: workloads_using and workload_specific_overrides are derived from the workbook and should be regenerated when
  parameter cells or descriptions change.
parameters:
  num_iterations:
    description: Number of timed repetitions.
    data_type: integer
    units: iterations
    default_value: workload-specific
    valid_range: positive integer
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 104
    - 105
    - 107
    - 109
    - 110
    - 113
    - 114
    - 115
    - 116
    - 117
    - 118
    - 119
    - 120
    - 121
    - 122
    - 123
    - 125
    - 126
    - 204
    - 205
    - 207
    - 209
    - 210
    - 213
    - 214
    - 215
    - 216
    - 217
    - 218
    - 219
    - 220
    - 221
    - 222
    - 223
    - 225
    - 226
    - 304
    - 305
    - 307
    - 309
    - 310
    - 313
    - 314
    - 315
    - 316
    - 317
    - 318
    - 319
    - 320
    - 321
    - 322
    - 323
    - 325
    - 326
    workload_specific_overrides:
      '104':
        workload_name: Silent Data Corruption (SDC)
        meaning: full passes over the memory region (default 2)
      '105':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: executions with randomized inputs per test case
      '107':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: timed repetitions per kernel type
      '109':
        workload_name: multichase Pointer-Chase
        meaning: pointer-chase operations per thread (default 10,000,000)
      '110':
        workload_name: NUMA Cache Latency Sweep
        meaning: full traversals of the working-set buffer
      '113':
        workload_name: GUPS Random Memory
        meaning: repetitions of the main update loop (each loop performs num_updates operations)
      '114':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: timed copy operations per transfer size
      '115':
        workload_name: BabelStream HBM Bandwidth
        meaning: timed repetitions per kernel
      '116':
        workload_name: RCCL Bandwidth Test
        meaning: timed repetitions per message size
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: timed repetitions of the batched GEMM
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: total timed convolution operations (default 100)
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: total gemm or conv2d kernel launches measured
      '120':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: complete HPL solves on freshly generated data
      '121':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: timed training steps; smoke runs 3, baseline runs at least 10, and extended runs at least 30. Optimizer is
          fixed SGD with one forward/backward pass per update
      '122':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: total inference steps
      '123':
        workload_name: BERT Inference Sweep
        meaning: timed inference iterations per configuration—smoke 5, baseline 20, extended 50
      '125':
        workload_name: DistilBERT NLP Training Baseline
        meaning: training steps selected by the profile—smoke 2, baseline 10, extended 30
      '126':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: timed forward passes (default 50)
      '204':
        workload_name: Silent Data Corruption (SDC)
        meaning: complete passes
      '205':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: executions with randomized inputs per test case
      '207':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: timed repetitions per kernel type
      '209':
        workload_name: multichase Pointer-Chase
        meaning: pointer-chase operations per thread (default 10,000,000)
      '210':
        workload_name: NUMA Cache Latency Sweep
        meaning: full traversals of the working-set buffer
      '213':
        workload_name: GUPS Random Memory
        meaning: repetitions of the main update loop (each loop performs num_updates operations)
      '214':
        workload_name: CUDA Memcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: timed copy operations per transfer size
      '215':
        workload_name: BabelStream HBM Bandwidth
        meaning: timed repetitions per kernel
      '216':
        workload_name: NCCL Bandwidth Test
        meaning: timed repetitions per message size
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: timed repetitions of the batched GEMM
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: timed operations
      '219':
        workload_name: TorchBench Microkernels
        meaning: total gemm or conv2d kernel launches measured
      '220':
        workload_name: NVIDIA HPL FP64 (High Performance Linpack)
        meaning: complete HPL solves on freshly generated data
      '221':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: total training steps (forward + backward + optimizer)
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: total inference steps
      '223':
        workload_name: BERT Inference Sweep
        meaning: timed inference steps
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: timed optimizer steps after warmup; may equal or replace num_epochs for profile sizing
      '226':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: timed forward passes (default 50)
      '304':
        workload_name: Silent Data Corruption (SDC)
        meaning: full passes over the memory region (default 2)
      '305':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: executions with randomized inputs per test case
      '307':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: timed repetitions per kernel type
      '309':
        workload_name: multichase Pointer-Chase
        meaning: pointer-chase operations per thread (default 10,000,000)
      '310':
        workload_name: NUMA Cache Latency Sweep
        meaning: full traversals of the working-set buffer
      '313':
        workload_name: GUPS Random Memory
        meaning: repetitions of the main update loop (each loop performs num_updates operations)
      '314':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: timed copy operations per transfer size
      '315':
        workload_name: BabelStream HBM Bandwidth
        meaning: timed repetitions per kernel
      '316':
        workload_name: RCCL Bandwidth Test
        meaning: timed repetitions per message size
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: timed repetitions of the batched GEMM
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: total timed convolution operations (default 100)
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: total gemm or conv2d kernel launches measured
      '320':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: complete HPL solves on freshly generated data
      '321':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: timed training steps; smoke runs 3, baseline runs at least 10, and extended runs at least 30. Optimizer is
          fixed SGD with one forward/backward pass per update
      '322':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: total inference steps
      '323':
        workload_name: BERT Inference Sweep
        meaning: timed inference iterations per configuration—smoke 5, baseline 20, extended 50
      '325':
        workload_name: DistilBERT NLP Training Baseline
        meaning: training steps selected by the profile—smoke 2, baseline 10, extended 30
      '326':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: timed forward passes (default 50)
    cli_config_key: workload-specific
    interaction_precedence: termination control; may interact with duration
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  dtype:
    description: Tensor, element, or storage data type.
    data_type: string
    units: format token
    default_value: workload-specific
    valid_range: workload-specific supported dtype token
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 105
    - 107
    - 115
    - 117
    - 118
    - 119
    - 122
    - 123
    - 126
    - 127
    - 128
    - 129
    - 130
    - 131
    - 205
    - 207
    - 215
    - 217
    - 218
    - 219
    - 222
    - 223
    - 226
    - 227
    - 228
    - 229
    - 230
    - 231
    - 305
    - 307
    - 315
    - 317
    - 318
    - 319
    - 322
    - 323
    - 326
    - 327
    - 328
    - 329
    - 330
    - 331
    workload_specific_overrides:
      '105':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: tensor precision — BF16, FP16, or FP32
      '107':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: floating-point precision — FP64 or FP32
      '115':
        workload_name: BabelStream HBM Bandwidth
        meaning: floating-point precision — FP32 or FP64
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: 'compute precision (f16_r, f32_r, or f64_r in the current sweep). M, N, K: matrix dimensions (A is M×K, B
          is K×N, C is M×N). transposeA, transposeB: whether to transpose A and/or B before multiply. alpha, beta: scalar
          multipliers for the product and accumulator. lda / ldb / ldc: leading dimension (stride in contiguous elements)
          for columns of A, B, C'
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: tensor precision (default FP16)
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: precision for inputs, weights, and accumulations — FP16, FP32, etc
      '122':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: tensor storage precision in GPU memory — BF16
      '123':
        workload_name: BERT Inference Sweep
        meaning: model and tensor storage precision in GPU memory — f16_r or f32_r
      '126':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: precision for activations and weights — BF16, FP16, or FP32
      '127':
        workload_name: KV Cache Stress Test
        meaning: compute precision (default BF16)
      '128':
        workload_name: vLLM Inference Throughput & Latency
        meaning: compute precision — BF16, FP16, or FP32
      '129':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: compute precision — BF16, FP16, or FP32
      '130':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: server compute precision (BF16/FP16/FP32)
      '131':
        workload_name: SGLang Serving Latency Benchmark
        meaning: server compute precision
      '205':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: tensor precision — BF16, FP16, or FP32
      '207':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: floating-point precision — FP64 or FP32
      '215':
        workload_name: BabelStream HBM Bandwidth
        meaning: floating-point precision — FP32 or FP64
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: 'compute precision (fp16, bf16, fp32, fp64, etc.). M, N, K: matrix dimensions (A is M×K, B is K×N, C is M×N).
          transposeA, transposeB: whether to transpose A and/or B before multiply. alpha, beta: scalar multipliers for the
          product and accumulator. lda / ldb / ldc: leading dimension (stride in contiguous elements) for columns of A, B,
          C'
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: FP16, BF16, FP32, or TF32-enabled FP32
      '219':
        workload_name: TorchBench Microkernels
        meaning: precision for inputs, weights, and accumulations — FP16, FP32, etc
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: tensor storage precision in GPU memory — BF16
      '223':
        workload_name: BERT Inference Sweep
        meaning: tensor storage precision in GPU memory — tf.float16 or tf.float32
      '226':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: precision for activations and weights — BF16, FP16, or FP32
      '227':
        workload_name: KV Cache Stress Test
        meaning: compute precision (default BF16)
      '228':
        workload_name: vLLM Inference Throughput & Latency
        meaning: compute precision — BF16, FP16, or FP32
      '229':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: compute precision — BF16, FP16, or FP32
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: compute precision — BF16, FP16, or FP32
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: compute precision — BF16, FP16, or FP32
      '305':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: tensor precision — BF16, FP16, or FP32
      '307':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: floating-point precision — FP64 or FP32
      '315':
        workload_name: BabelStream HBM Bandwidth
        meaning: floating-point precision — FP32 or FP64
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: 'compute precision (f16_r, f32_r, or f64_r in the current sweep). M, N, K: matrix dimensions (A is M×K, B
          is K×N, C is M×N). transposeA, transposeB: whether to transpose A and/or B before multiply. alpha, beta: scalar
          multipliers for the product and accumulator. lda / ldb / ldc: leading dimension (stride in contiguous elements)
          for columns of A, B, C'
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: tensor precision (default FP16)
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: precision for inputs, weights, and accumulations — FP16, FP32, etc
      '322':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: tensor storage precision in GPU memory — BF16
      '323':
        workload_name: BERT Inference Sweep
        meaning: model and tensor storage precision in GPU memory — f16_r or f32_r
      '326':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: precision for activations and weights — BF16, FP16, or FP32
      '327':
        workload_name: KV Cache Stress Test
        meaning: compute precision (default BF16)
      '328':
        workload_name: vLLM Inference Throughput & Latency
        meaning: compute precision — BF16, FP16, or FP32
      '329':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: compute precision — BF16, FP16, or FP32
      '330':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: server compute precision (BF16/FP16/FP32)
      '331':
        workload_name: SGLang Serving Latency Benchmark
        meaning: server compute precision
    cli_config_key: workload-specific
    interaction_precedence: keep distinct from precision when storage and compute formats differ
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  warmup_iters:
    description: Untimed warm-up repetitions before measurement.
    data_type: integer
    units: iterations
    default_value: workload-specific
    valid_range: integer >= 0
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 109
    - 114
    - 115
    - 116
    - 117
    - 118
    - 119
    - 120
    - 121
    - 122
    - 123
    - 126
    - 209
    - 214
    - 215
    - 216
    - 217
    - 218
    - 219
    - 220
    - 221
    - 222
    - 223
    - 225
    - 226
    - 309
    - 314
    - 315
    - 316
    - 317
    - 318
    - 319
    - 320
    - 321
    - 322
    - 323
    - 326
    workload_specific_overrides:
      '109':
        workload_name: multichase Pointer-Chase
        meaning: unmeasured iterations before timing starts (default 1,000,000)
      '114':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: un-timed warm-up passes before measurement
      '115':
        workload_name: BabelStream HBM Bandwidth
        meaning: un-timed warm-up iterations before measurement loop
      '116':
        workload_name: RCCL Bandwidth Test
        meaning: un-timed warm-up passes
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: 'untimed warm-up passes before measurement—smoke: 2, baseline: 5, extended: 10'
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: un-timed warm-up passes (default 10)
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: initial unmeasured kernel launches before timing
      '120':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: un-timed solves before measurement
      '121':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: untimed batches before timing starts (default 1)
      '122':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: un-timed batches before timing starts
      '123':
        workload_name: BERT Inference Sweep
        meaning: initial untimed inference iterations before measurement (default 2)
      '126':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: initial un-timed passes before measurement (default 5)
      '209':
        workload_name: multichase Pointer-Chase
        meaning: unmeasured iterations before timing starts (default 1,000,000)
      '214':
        workload_name: CUDA Memcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: un-timed warm-up passes before measurement
      '215':
        workload_name: BabelStream HBM Bandwidth
        meaning: un-timed warm-up iterations before measurement loop
      '216':
        workload_name: NCCL Bandwidth Test
        meaning: un-timed warm-up passes
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: un-timed warm-up passes before measurement
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: un-timed passes
      '219':
        workload_name: TorchBench Microkernels
        meaning: initial unmeasured kernel launches before timing
      '220':
        workload_name: NVIDIA HPL FP64 (High Performance Linpack)
        meaning: un-timed solves before measurement
      '221':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: un-timed batches before timing starts
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: un-timed batches before timing starts
      '223':
        workload_name: BERT Inference Sweep
        meaning: initial un-timed iterations before measurement
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: initial training steps with a linearly ramped learning rate (default 500)
      '226':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: initial un-timed passes before measurement (default 5)
      '309':
        workload_name: multichase Pointer-Chase
        meaning: unmeasured iterations before timing starts (default 1,000,000)
      '314':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: un-timed warm-up passes before measurement
      '315':
        workload_name: BabelStream HBM Bandwidth
        meaning: un-timed warm-up iterations before measurement loop
      '316':
        workload_name: RCCL Bandwidth Test
        meaning: un-timed warm-up passes
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: 'untimed warm-up passes before measurement—smoke: 2, baseline: 5, extended: 10'
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: un-timed warm-up passes (default 10)
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: initial unmeasured kernel launches before timing
      '320':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: un-timed solves before measurement
      '321':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: untimed batches before timing starts (default 1)
      '322':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: un-timed batches before timing starts
      '323':
        workload_name: BERT Inference Sweep
        meaning: initial untimed inference iterations before measurement (default 2)
      '326':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: initial un-timed passes before measurement (default 5)
    cli_config_key: workload-specific
    interaction_precedence: excluded from measured iteration counts
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  batch_size:
    description: Number of items processed together.
    data_type: integer
    units: items per batch
    default_value: workload-specific
    valid_range: integer >= 1
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 119
    - 121
    - 122
    - 123
    - 125
    - 126
    - 132
    - 218
    - 219
    - 221
    - 222
    - 223
    - 224
    - 225
    - 226
    - 232
    - 318
    - 319
    - 321
    - 322
    - 323
    - 325
    - 326
    - 332
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: N — images per pass (default 32)
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: samples processed in parallel per kernel launch
      '121':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: images processed per forward/backward pass (default 8)
      '122':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: images processed simultaneously per forward pass
      '123':
        workload_name: BERT Inference Sweep
        meaning: sequences processed simultaneously per forward pass
      '125':
        workload_name: DistilBERT NLP Training Baseline
        meaning: input samples processed per forward/backward pass (default 8)
      '126':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: input sequences processed simultaneously per forward pass (default 8)
      '132':
        workload_name: RAG Pipeline Sweep
        meaning: queries processed together per batch (default 8)
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '219':
        workload_name: TorchBench Microkernels
        meaning: samples processed in parallel per kernel launch
      '221':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: images processed per forward/backward pass
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: images processed simultaneously per forward pass
      '223':
        workload_name: BERT Inference Sweep
        meaning: sequences processed simultaneously per forward pass
      '224':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: images processed simultaneously in a single forward pass
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: input samples processed simultaneously per forward pass per GPU
      '226':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: input sequences processed simultaneously per forward pass (default 8)
      '232':
        workload_name: RAG Pipeline Sweep
        meaning: queries processed together per batch (default 8)
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: N — images per pass (default 32)
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: samples processed in parallel per kernel launch
      '321':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: images processed per forward/backward pass (default 8)
      '322':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: images processed simultaneously per forward pass
      '323':
        workload_name: BERT Inference Sweep
        meaning: sequences processed simultaneously per forward pass
      '325':
        workload_name: DistilBERT NLP Training Baseline
        meaning: input samples processed per forward/backward pass (default 8)
      '326':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: input sequences processed simultaneously per forward pass (default 8)
      '332':
        workload_name: RAG Pipeline Sweep
        meaning: queries processed together per batch (default 8)
    cli_config_key: workload-specific
    interaction_precedence: constrained by available memory and sometimes num_gpus
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  seed:
    description: Random seed used for reproducibility.
    data_type: integer
    units: seed value
    default_value: workload-specific
    valid_range: integer >= 0
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 104
    - 105
    - 113
    - 119
    - 124
    - 126
    - 127
    - 130
    - 131
    - 204
    - 205
    - 213
    - 219
    - 224
    - 226
    - 227
    - 304
    - 305
    - 313
    - 319
    - 324
    - 326
    - 327
    - 330
    - 331
    workload_specific_overrides:
      '104':
        workload_name: Silent Data Corruption (SDC)
        meaning: random seed for randomized patterns or address selection, enabling repeatable runs (default 0)
      '105':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '113':
        workload_name: GUPS Random Memory
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '124':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '126':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '127':
        workload_name: KV Cache Stress Test
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '130':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: RNG seed for reproducible synthetic prompts
      '131':
        workload_name: SGLang Serving Latency Benchmark
        meaning: RNG seed
      '204':
        workload_name: Silent Data Corruption (SDC)
        meaning: repeatable random-pattern seed
      '205':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '213':
        workload_name: GUPS Random Memory
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '219':
        workload_name: TorchBench Microkernels
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '224':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '226':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '227':
        workload_name: KV Cache Stress Test
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '304':
        workload_name: Silent Data Corruption (SDC)
        meaning: random seed for randomized patterns or address selection, enabling repeatable runs (default 0)
      '305':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '313':
        workload_name: GUPS Random Memory
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '324':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '326':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '327':
        workload_name: KV Cache Stress Test
        meaning: random number generator seed for reproducibility (e.g. 0, 42, 1234)
      '330':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: RNG seed for reproducible synthetic prompts
      '331':
        workload_name: SGLang Serving Latency Benchmark
        meaning: RNG seed
    cli_config_key: workload-specific
    interaction_precedence: same seed does not guarantee identical hardware timing
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  device_id:
    description: Target accelerator or device identifier.
    data_type: integer or string
    units: device identifier
    default_value: workload-specific
    valid_range: workload-specific valid device
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 104
    - 105
    - 114
    - 115
    - 119
    - 122
    - 126
    - 204
    - 205
    - 214
    - 215
    - 219
    - 222
    - 226
    - 304
    - 305
    - 314
    - 315
    - 319
    - 322
    - 326
    workload_specific_overrides:
      '104':
        workload_name: Silent Data Corruption (SDC)
        meaning: GPU device index to test (default 0)
      '105':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: logical GPU selection for the PyTorch ROCm device
      '114':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: GPU device index (default 0)
      '115':
        workload_name: BabelStream HBM Bandwidth
        meaning: GPU device index (default 0)
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: GPU device index (default 0)
      '122':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: GPU device identifier (e.g
      '126':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: GPU device index (default 0)
      '204':
        workload_name: Silent Data Corruption (SDC)
        meaning: CUDA device index
      '205':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: logical GPU selection for the PyTorch CUDA device
      '214':
        workload_name: CUDA Memcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: GPU device index (default 0)
      '215':
        workload_name: BabelStream HBM Bandwidth
        meaning: GPU device index (default 0)
      '219':
        workload_name: TorchBench Microkernels
        meaning: GPU device index (default 0)
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: GPU device identifier (e.g
      '226':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: GPU device index (default 0)
      '304':
        workload_name: Silent Data Corruption (SDC)
        meaning: GPU device index to test (default 0)
      '305':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: logical GPU selection for the PyTorch ROCm device
      '314':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: GPU device index (default 0)
      '315':
        workload_name: BabelStream HBM Bandwidth
        meaning: GPU device index (default 0)
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: GPU device index (default 0)
      '322':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: GPU device identifier (e.g
      '326':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: GPU device index (default 0)
    cli_config_key: workload-specific
    interaction_precedence: must refer to a visible device
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  output_format:
    description: Format used for benchmark output.
    data_type: string
    units: format token
    default_value: workload-specific
    valid_range: one of the workload-supported formats
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 101
    - 102
    - 103
    - 104
    - 111
    - 112
    - 115
    - 201
    - 202
    - 203
    - 204
    - 211
    - 212
    - 215
    - 301
    - 302
    - 303
    - 304
    - 311
    - 312
    - 315
    workload_specific_overrides:
      '101':
        workload_name: System Config Verification
        meaning: results format — csv or plain text (default csv)
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: results format — csv or plain text (default csv)
      '103':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: results format — csv or plain text (default csv)
      '104':
        workload_name: Silent Data Corruption (SDC)
        meaning: error log format — csv or plain text (default csv)
      '111':
        workload_name: Linux perf PMU Analysis Harness
        meaning: results format — csv or plain text
      '112':
        workload_name: lmbench Microbench Suite
        meaning: results format — csv or plain text. lat_pipe, lat_unix, and lat_proc remain Command Tools binaries, not sweep
          parameters
      '115':
        workload_name: BabelStream HBM Bandwidth
        meaning: true = CSV format, false = plain text
      '201':
        workload_name: System Config Verification
        meaning: csv or plain text
      '202':
        workload_name: NVIDIA System Validation (clocks, temperature, etc.)
        meaning: csv or plain text
      '203':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: results format — csv or plain text (default csv)
      '204':
        workload_name: Silent Data Corruption (SDC)
        meaning: csv or plain text
      '211':
        workload_name: Linux perf PMU Analysis Harness
        meaning: results format — csv or plain text
      '212':
        workload_name: lmbench Microbench Suite
        meaning: results format — csv or plain text
      '215':
        workload_name: BabelStream HBM Bandwidth
        meaning: true = CSV format, false = plain text
      '301':
        workload_name: System Config Verification
        meaning: results format — csv or plain text (default csv)
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: results format — csv or plain text (default csv)
      '303':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: results format — csv or plain text (default csv)
      '304':
        workload_name: Silent Data Corruption (SDC)
        meaning: error log format — csv or plain text (default csv)
      '311':
        workload_name: Linux perf PMU Analysis Harness
        meaning: results format — csv or plain text
      '312':
        workload_name: lmbench Microbench Suite
        meaning: results format — csv or plain text. lat_pipe, lat_unix, and lat_proc remain Command Tools binaries, not sweep
          parameters
      '315':
        workload_name: BabelStream HBM Bandwidth
        meaning: true = CSV format, false = plain text
    cli_config_key: workload-specific
    interaction_precedence: must agree with parser expectations
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  model_name:
    description: Model or weights identifier to load.
    data_type: string
    units: model identifier
    default_value: workload-specific
    valid_range: non-empty supported model identifier
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 123
    - 124
    - 125
    - 127
    - 128
    - 129
    - 223
    - 224
    - 225
    - 227
    - 228
    - 229
    - 230
    - 231
    - 323
    - 324
    - 325
    - 327
    - 328
    - 329
    workload_specific_overrides:
      '123':
        workload_name: BERT Inference Sweep
        meaning: Hugging Face model identifier
      '124':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: SDXL model variant weights (default sdxl-base-1.0)
      '125':
        workload_name: DistilBERT NLP Training Baseline
        meaning: Hugging Face base model for sequence classification (default distilbert-base-uncased)
      '127':
        workload_name: KV Cache Stress Test
        meaning: LLM weights to load (e.g. Mistral-7B-v0.3)
      '128':
        workload_name: vLLM Inference Throughput & Latency
        meaning: LLM weights to load (e.g. Mistral)
      '129':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: LLM weights to load (e.g. Mistral)
      '223':
        workload_name: BERT Inference Sweep
        meaning: BERT variant to load via HuggingFace TF (default bert-base-uncased)
      '224':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: SDXL model variant weights (default sdxl-base-1.0)
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: base model weights for classification fine-tuning (default distilbert-base-uncased)
      '227':
        workload_name: KV Cache Stress Test
        meaning: LLM weights to load (e.g. Mistral-7B-v0.3)
      '228':
        workload_name: vLLM Inference Throughput & Latency
        meaning: LLM weights to load (e.g. Mistral)
      '229':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: LLM weights to load (e.g. Mistral)
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: LLM weights to load (e.g. Mistral-7B-v0.3)
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: LLM weights to load (e.g. Mistral-7B-v0.3)
      '323':
        workload_name: BERT Inference Sweep
        meaning: Hugging Face model identifier
      '324':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: SDXL model variant weights (default sdxl-base-1.0)
      '325':
        workload_name: DistilBERT NLP Training Baseline
        meaning: Hugging Face base model for sequence classification (default distilbert-base-uncased)
      '327':
        workload_name: KV Cache Stress Test
        meaning: LLM weights to load (e.g. Mistral-7B-v0.3)
      '328':
        workload_name: vLLM Inference Throughput & Latency
        meaning: LLM weights to load (e.g. Mistral)
      '329':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: LLM weights to load (e.g. Mistral)
    cli_config_key: workload-specific
    interaction_precedence: may require model download and available memory
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  input_len:
    description: Input or prompt token length.
    data_type: integer
    units: tokens
    default_value: workload-specific
    valid_range: integer >= 0; workload-specific maximum
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 127
    - 128
    - 129
    - 130
    - 131
    - 227
    - 228
    - 229
    - 230
    - 231
    - 327
    - 328
    - 329
    - 330
    - 331
    workload_specific_overrides:
      '127':
        workload_name: KV Cache Stress Test
        meaning: input tokens per request (default 4096)
      '128':
        workload_name: vLLM Inference Throughput & Latency
        meaning: input tokens per request
      '129':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: input tokens per request
      '130':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: target input tokens per prompt
      '131':
        workload_name: SGLang Serving Latency Benchmark
        meaning: target input tokens per request
      '227':
        workload_name: KV Cache Stress Test
        meaning: input tokens per request (default 4096)
      '228':
        workload_name: vLLM Inference Throughput & Latency
        meaning: input tokens per request
      '229':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: input tokens per request
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: tokens per input prompt
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: tokens per input prompt
      '327':
        workload_name: KV Cache Stress Test
        meaning: input tokens per request (default 4096)
      '328':
        workload_name: vLLM Inference Throughput & Latency
        meaning: input tokens per request
      '329':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: input tokens per request
      '330':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: target input tokens per prompt
      '331':
        workload_name: SGLang Serving Latency Benchmark
        meaning: target input tokens per request
    cli_config_key: workload-specific
    interaction_precedence: distinct from total sequence length
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_threads:
    description: Execution threads issuing work.
    data_type: integer
    units: threads
    default_value: workload-specific
    valid_range: integer >= 1
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 103
    - 107
    - 109
    - 110
    - 113
    - 203
    - 207
    - 209
    - 210
    - 213
    - 303
    - 307
    - 309
    - 310
    - 313
    workload_specific_overrides:
      '103':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: CPU worker threads spawned by stress-ng (default 16)
      '107':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: CPU threads executing kernels in parallel
      '109':
        workload_name: multichase Pointer-Chase
        meaning: concurrent threads (default 4)
      '110':
        workload_name: NUMA Cache Latency Sweep
        meaning: concurrent threads issuing memory accesses
      '113':
        workload_name: GUPS Random Memory
        meaning: concurrent threads issuing updates
      '203':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: CPU worker threads spawned for stress (default 16)
      '207':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: CPU threads executing kernels in parallel
      '209':
        workload_name: multichase Pointer-Chase
        meaning: concurrent threads (default 4)
      '210':
        workload_name: NUMA Cache Latency Sweep
        meaning: concurrent threads issuing memory accesses
      '213':
        workload_name: GUPS Random Memory
        meaning: concurrent threads issuing updates
      '303':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: CPU worker threads spawned by stress-ng (default 16)
      '307':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: CPU threads executing kernels in parallel
      '309':
        workload_name: multichase Pointer-Chase
        meaning: concurrent threads (default 4)
      '310':
        workload_name: NUMA Cache Latency Sweep
        meaning: concurrent threads issuing memory accesses
      '313':
        workload_name: GUPS Random Memory
        meaning: concurrent threads issuing updates
    cli_config_key: workload-specific
    interaction_precedence: distinct from num_workers
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  output_len:
    description: Number of generated or output tokens.
    data_type: integer
    units: tokens
    default_value: workload-specific
    valid_range: integer >= 0
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 127
    - 128
    - 129
    - 130
    - 131
    - 227
    - 228
    - 229
    - 230
    - 231
    - 327
    - 328
    - 329
    - 330
    - 331
    workload_specific_overrides:
      '127':
        workload_name: KV Cache Stress Test
        meaning: tokens generated per request (default 128)
      '128':
        workload_name: vLLM Inference Throughput & Latency
        meaning: tokens generated per request
      '129':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: tokens generated per request
      '130':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: maximum generated tokens per response
      '131':
        workload_name: SGLang Serving Latency Benchmark
        meaning: generated tokens per request
      '227':
        workload_name: KV Cache Stress Test
        meaning: tokens generated per request (default 128)
      '228':
        workload_name: vLLM Inference Throughput & Latency
        meaning: tokens generated per request
      '229':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: tokens generated per request
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: tokens generated per request
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: tokens generated per request
      '327':
        workload_name: KV Cache Stress Test
        meaning: tokens generated per request (default 128)
      '328':
        workload_name: vLLM Inference Throughput & Latency
        meaning: tokens generated per request
      '329':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: tokens generated per request
      '330':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: maximum generated tokens per response
      '331':
        workload_name: SGLang Serving Latency Benchmark
        meaning: generated tokens per request
    cli_config_key: workload-specific
    interaction_precedence: input_len + output_len may be limited by model context
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  precision:
    description: Arithmetic or compute precision.
    data_type: string
    units: format token
    default_value: workload-specific
    valid_range: workload-specific supported precision token
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 120
    - 121
    - 122
    - 123
    - 124
    - 220
    - 221
    - 222
    - 223
    - 224
    - 320
    - 321
    - 322
    - 323
    - 324
    workload_specific_overrides:
      '120':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: fixed FP64
      '121':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: requested compute precision — FP16, BF16, or FP32; FP16 autocast is implemented, while BF16 is not currently
          implemented as BF16 autocast
      '122':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: compute precision via PyTorch AMP — BF16
      '123':
        workload_name: BERT Inference Sweep
        meaning: inference autocast mode — mixed_float16 or float32
      '124':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: model weight and activation precision — BF16, FP16, or FP32
      '220':
        workload_name: NVIDIA HPL FP64 (High Performance Linpack)
        meaning: fixed FP64
      '221':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: compute precision via PyTorch AMP — BF16, FP16, or FP32
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: compute precision via PyTorch AMP — BF16
      '223':
        workload_name: BERT Inference Sweep
        meaning: compute precision via tf.keras.mixed_precision — mixed_float16 or float32
      '224':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: model weight and activation precision — BF16, FP16, or FP32
      '320':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: fixed FP64
      '321':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: requested compute precision — FP16, BF16, or FP32; FP16 autocast is implemented, while BF16 is not currently
          implemented as BF16 autocast
      '322':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: compute precision via PyTorch AMP — BF16
      '323':
        workload_name: BERT Inference Sweep
        meaning: inference autocast mode — mixed_float16 or float32
      '324':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: model weight and activation precision — BF16, FP16, or FP32
    cli_config_key: workload-specific
    interaction_precedence: may differ from storage dtype
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  prompt_source:
    description: Source or generation mode for input prompts.
    data_type: string
    units: source identifier
    default_value: workload-specific
    valid_range: supported source token
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 123
    - 124
    - 128
    - 129
    - 219
    - 223
    - 224
    - 228
    - 229
    - 230
    - 231
    - 323
    - 324
    - 328
    - 329
    workload_specific_overrides:
      '123':
        workload_name: BERT Inference Sweep
        meaning: same as input_source for this BERT sweep (synthetic in-memory tokens)
      '124':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: source of text prompts
      '128':
        workload_name: vLLM Inference Throughput & Latency
        meaning: source of input prompts (synthetic)
      '129':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: source of input prompts (synthetic)
      '219':
        workload_name: TorchBench Microkernels
        meaning: unused TorchBench token retained for matrix compatibility; kernels use input_shape
      '223':
        workload_name: BERT Inference Sweep
        meaning: input generation method (random)
      '224':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: source of text prompts
      '228':
        workload_name: vLLM Inference Throughput & Latency
        meaning: source of input prompts (synthetic)
      '229':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: source of input prompts (synthetic)
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: source of input prompts (synthetic)
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: source of input prompts (synthetic)
      '323':
        workload_name: BERT Inference Sweep
        meaning: same as input_source for this BERT sweep (synthetic in-memory tokens)
      '324':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: source of text prompts
      '328':
        workload_name: vLLM Inference Throughput & Latency
        meaning: source of input prompts (synthetic)
      '329':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: source of input prompts (synthetic)
    cli_config_key: workload-specific
    interaction_precedence: determines whether prompts are synthetic or dataset-backed
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  page_size:
    description: Memory page size used by the workload.
    data_type: integer or string
    units: bytes or page token
    default_value: workload-specific
    valid_range: supported OS or benchmark page size
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 107
    - 109
    - 110
    - 113
    - 207
    - 209
    - 210
    - 213
    - 307
    - 309
    - 310
    - 313
    workload_specific_overrides:
      '107':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: OS page size — 4KB standard or 2MB huge pages
      '109':
        workload_name: multichase Pointer-Chase
        meaning: OS page size — 4KB, 2MB, or 1GB huge pages
      '110':
        workload_name: NUMA Cache Latency Sweep
        meaning: OS page size — 2MB or 1GB huge pages, or disabled (4KB standard)
      '113':
        workload_name: GUPS Random Memory
        meaning: OS page size — 4KB standard or 2MB huge pages
      '207':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: OS page size — 4KB standard or 2MB huge pages
      '209':
        workload_name: multichase Pointer-Chase
        meaning: OS page size — 4KB, 2MB, or 1GB huge pages
      '210':
        workload_name: NUMA Cache Latency Sweep
        meaning: OS page size — 2MB or 1GB huge pages, or disabled (4KB standard)
      '213':
        workload_name: GUPS Random Memory
        meaning: OS page size — 4KB standard or 2MB huge pages
      '307':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: OS page size — 4KB standard or 2MB huge pages
      '309':
        workload_name: multichase Pointer-Chase
        meaning: OS page size — 4KB, 2MB, or 1GB huge pages
      '310':
        workload_name: NUMA Cache Latency Sweep
        meaning: OS page size — 2MB or 1GB huge pages, or disabled (4KB standard)
      '313':
        workload_name: GUPS Random Memory
        meaning: OS page size — 4KB standard or 2MB huge pages
    cli_config_key: workload-specific
    interaction_precedence: units must be explicit because values may be written as 4K, 2M, or bytes
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  thread_affinity:
    description: Policy or CPU/NUMA placement for execution threads.
    data_type: string
    units: affinity specification
    default_value: workload-specific
    valid_range: supported affinity expression
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 107
    - 109
    - 110
    - 113
    - 207
    - 209
    - 210
    - 213
    - 307
    - 309
    - 310
    - 313
    workload_specific_overrides:
      '107':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: thread-to-core binding policy — none, core, socket, etc
      '109':
        workload_name: multichase Pointer-Chase
        meaning: CPU/NUMA/CCX pinning specification for each thread (e.g. "0-3")
      '110':
        workload_name: NUMA Cache Latency Sweep
        meaning: thread pinning policy on the source node — strict, local, none
      '113':
        workload_name: GUPS Random Memory
        meaning: thread-to-core binding policy — none, compact, node:0, etc
      '207':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: thread-to-core binding policy — none, core, socket, etc
      '209':
        workload_name: multichase Pointer-Chase
        meaning: CPU/NUMA/CCX pinning specification for each thread (e.g. "0-3")
      '210':
        workload_name: NUMA Cache Latency Sweep
        meaning: thread pinning policy on the source node — strict, local, none
      '213':
        workload_name: GUPS Random Memory
        meaning: thread-to-core binding policy — none, compact, node:0, etc
      '307':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: thread-to-core binding policy — none, core, socket, etc
      '309':
        workload_name: multichase Pointer-Chase
        meaning: CPU/NUMA/CCX pinning specification for each thread (e.g. "0-3")
      '310':
        workload_name: NUMA Cache Latency Sweep
        meaning: thread pinning policy on the source node — strict, local, none
      '313':
        workload_name: GUPS Random Memory
        meaning: thread-to-core binding policy — none, compact, node:0, etc
    cli_config_key: workload-specific
    interaction_precedence: interacts with NUMA placement and reproducibility
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_gpus:
    description: Number of GPUs participating in the workload.
    data_type: integer
    units: GPUs
    default_value: workload-specific
    valid_range: integer >= 1 and <= visible GPUs
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 116
    - 120
    - 123
    - 216
    - 220
    - 222
    - 223
    - 225
    - 316
    - 320
    - 323
    workload_specific_overrides:
      '116':
        workload_name: RCCL Bandwidth Test
        meaning: GPUs participating in the collective (default 1)
      '120':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: GPUs participating (must equal P×Q; default 1)
      '123':
        workload_name: BERT Inference Sweep
        meaning: exactly one GPU; multi-GPU execution is not supported
      '216':
        workload_name: NCCL Bandwidth Test
        meaning: GPUs participating in the collective (default 1)
      '220':
        workload_name: NVIDIA HPL FP64 (High Performance Linpack)
        meaning: GPUs participating (must equal P×Q; default 1)
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: GPUs participating in the test (default 1)
      '223':
        workload_name: BERT Inference Sweep
        meaning: PyTorch-visible GPUs in the test (default 1)
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: GPUs participating in the test (default 1)
      '316':
        workload_name: RCCL Bandwidth Test
        meaning: GPUs participating in the collective (default 1)
      '320':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: GPUs participating (must equal P×Q; default 1)
      '323':
        workload_name: BERT Inference Sweep
        meaning: exactly one GPU; multi-GPU execution is not supported
    cli_config_key: workload-specific
    interaction_precedence: may constrain tensor parallelism and process-grid dimensions
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  max_concurrency:
    description: Maximum client-side requests in flight.
    data_type: integer
    units: in-flight requests
    default_value: workload-specific
    valid_range: integer >= 1
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 128
    - 129
    - 131
    - 228
    - 229
    - 230
    - 231
    - 328
    - 329
    - 331
    workload_specific_overrides:
      '128':
        workload_name: vLLM Inference Throughput & Latency
        meaning: maximum client-side requests in flight simultaneously
      '129':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: maximum client-side requests in flight simultaneously
      '131':
        workload_name: SGLang Serving Latency Benchmark
        meaning: maximum requests in flight
      '228':
        workload_name: vLLM Inference Throughput & Latency
        meaning: maximum client-side requests in flight simultaneously
      '229':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: maximum client-side requests in flight simultaneously
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: maximum client-side requests in flight simultaneously
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: maximum client-side requests in flight simultaneously
      '328':
        workload_name: vLLM Inference Throughput & Latency
        meaning: maximum client-side requests in flight simultaneously
      '329':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: maximum client-side requests in flight simultaneously
      '331':
        workload_name: SGLang Serving Latency Benchmark
        meaning: maximum requests in flight
    cli_config_key: workload-specific
    interaction_precedence: client queue limit; distinct from server and engine capacity
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  block_size:
    description: Work unit or block size.
    data_type: integer or string
    units: workload-specific
    default_value: workload-specific
    valid_range: positive workload-specific value
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 115
    - 120
    - 206
    - 215
    - 220
    - 306
    - 315
    - 320
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: data size per I/O request (e.g. 4k, 8k)
      '115':
        workload_name: BabelStream HBM Bandwidth
        meaning: HIP/CUDA threads per block (default 256)
      '120':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: NB panel tile size (e.g. 256, 512)
      '206':
        workload_name: FIO NVMe Sweep
        meaning: data size per I/O request (e.g. 4k, 8k)
      '215':
        workload_name: BabelStream HBM Bandwidth
        meaning: CUDA threads per block (default 256)
      '220':
        workload_name: NVIDIA HPL FP64 (High Performance Linpack)
        meaning: NB panel tile size (e.g. 256, 512)
      '306':
        workload_name: FIO NVMe Sweep
        meaning: data size per I/O request (e.g. 4k, 8k)
      '315':
        workload_name: BabelStream HBM Bandwidth
        meaning: HIP/CUDA threads per block (default 256)
      '320':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: NB panel tile size (e.g. 256, 512)
    cli_config_key: workload-specific
    interaction_precedence: 'meaning varies: I/O bytes, GPU threads, matrix tile, or panel size'
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  duration:
    description: Wall-clock or sampling duration.
    data_type: number
    units: seconds
    default_value: workload-specific
    valid_range: number > 0
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 103
    - 104
    - 111
    - 203
    - 204
    - 211
    - 303
    - 304
    - 311
    workload_specific_overrides:
      '103':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: total run time in seconds (default smoke=5, baseline=30, extended=60)
      '104':
        workload_name: Silent Data Corruption (SDC)
        meaning: hard wall-clock cap in milliseconds; bounds the test to a fixed time window regardless of iteration count
          (default 240000 ms / 4 min)
      '111':
        workload_name: Linux perf PMU Analysis Harness
        meaning: total profiling duration in seconds (default smoke=2, baseline=10, extended=30)
      '203':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: total run time in seconds (default 300)
      '204':
        workload_name: Silent Data Corruption (SDC)
        meaning: wall-clock cap
      '211':
        workload_name: Linux perf PMU Analysis Harness
        meaning: total profiling duration in seconds (default 60)
      '303':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: total run time in seconds (default smoke=5, baseline=30, extended=60)
      '304':
        workload_name: Silent Data Corruption (SDC)
        meaning: hard wall-clock cap in milliseconds; bounds the test to a fixed time window regardless of iteration count
          (default 240000 ms / 4 min)
      '311':
        workload_name: Linux perf PMU Analysis Harness
        meaning: total profiling duration in seconds (default smoke=2, baseline=10, extended=30)
    cli_config_key: workload-specific
    interaction_precedence: must define precedence or mutual exclusion with num_iterations
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  ecc_check:
    description: Whether ECC error telemetry is collected or validated.
    data_type: boolean
    units: boolean
    default_value: workload-specific
    valid_range: true or false
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 102
    - 103
    - 104
    - 202
    - 203
    - 204
    - 302
    - 303
    - 304
    workload_specific_overrides:
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: reads ECC corrected/uncorrected error counters for HBM (default true)
      '103':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: configured ECC validation field; active ECC polling is not currently implemented (default true)
      '104':
        workload_name: Silent Data Corruption (SDC)
        meaning: poll corrected and uncorrected ECC counters during the test (default true)
      '202':
        workload_name: NVIDIA System Validation (clocks, temperature, etc.)
        meaning: reads corrected/uncorrected ECC counters
      '203':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: poll for ECC memory errors on GPU (default true)
      '204':
        workload_name: Silent Data Corruption (SDC)
        meaning: polls volatile and aggregate ECC counters
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: reads ECC corrected/uncorrected error counters for HBM (default true)
      '303':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: configured ECC validation field; active ECC polling is not currently implemented (default true)
      '304':
        workload_name: Silent Data Corruption (SDC)
        meaning: poll corrected and uncorrected ECC counters during the test (default true)
    cli_config_key: workload-specific
    interaction_precedence: may require supported hardware telemetry
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  gpu_memory_utilization:
    description: Fraction of GPU memory the runtime may use.
    data_type: number
    units: fraction
    default_value: workload-specific
    valid_range: 0 < value <= 1
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 127
    - 128
    - 129
    - 227
    - 228
    - 229
    - 327
    - 328
    - 329
    workload_specific_overrides:
      '127':
        workload_name: KV Cache Stress Test
        meaning: fraction of GPU memory vLLM may use (default 0.9 = 90%)
      '128':
        workload_name: vLLM Inference Throughput & Latency
        meaning: fraction of available GPU memory vLLM may use (default 0.9 = 90%)
      '129':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: fraction of available GPU memory vLLM may use (default 0.9 = 90%)
      '227':
        workload_name: KV Cache Stress Test
        meaning: fraction of GPU memory vLLM may use (default 0.9 = 90%)
      '228':
        workload_name: vLLM Inference Throughput & Latency
        meaning: fraction of available GPU memory vLLM may use (default 0.9 = 90%)
      '229':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: fraction of available GPU memory vLLM may use (default 0.9 = 90%)
      '327':
        workload_name: KV Cache Stress Test
        meaning: fraction of GPU memory vLLM may use (default 0.9 = 90%)
      '328':
        workload_name: vLLM Inference Throughput & Latency
        meaning: fraction of available GPU memory vLLM may use (default 0.9 = 90%)
      '329':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: fraction of available GPU memory vLLM may use (default 0.9 = 90%)
    cli_config_key: workload-specific
    interaction_precedence: must leave sufficient memory for runtime overhead
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_prompts:
    description: Total prompts or requests submitted.
    data_type: integer
    units: prompts
    default_value: workload-specific
    valid_range: integer >= 1
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 127
    - 130
    - 131
    - 227
    - 230
    - 231
    - 327
    - 330
    - 331
    workload_specific_overrides:
      '127':
        workload_name: KV Cache Stress Test
        meaning: total requests submitted (default 200)
      '130':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: total sequential prompt-response transactions
      '131':
        workload_name: SGLang Serving Latency Benchmark
        meaning: total benchmark requests
      '227':
        workload_name: KV Cache Stress Test
        meaning: total requests submitted (default 200)
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: total prompts submitted during the run
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: total prompts submitted during the run
      '327':
        workload_name: KV Cache Stress Test
        meaning: total requests submitted (default 200)
      '330':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: total sequential prompt-response transactions
      '331':
        workload_name: SGLang Serving Latency Benchmark
        meaning: total benchmark requests
    cli_config_key: workload-specific
    interaction_precedence: interacts with request_rate and concurrency
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  sequence_len:
    description: Total input sequence or context length.
    data_type: integer
    units: tokens
    default_value: workload-specific
    valid_range: integer >= 1; workload-specific maximum
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 123
    - 125
    - 126
    - 223
    - 225
    - 226
    - 323
    - 325
    - 326
    workload_specific_overrides:
      '123':
        workload_name: BERT Inference Sweep
        meaning: tokens per synthetic input sequence
      '125':
        workload_name: DistilBERT NLP Training Baseline
        meaning: maximum tokens per input sample (default 128)
      '126':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: tokens per input sequence / context window (default 512)
      '223':
        workload_name: BERT Inference Sweep
        meaning: tokens per input sequence
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: tokens per input sample
      '226':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: tokens per input sequence / context window (default 512)
      '323':
        workload_name: BERT Inference Sweep
        meaning: tokens per synthetic input sequence
      '325':
        workload_name: DistilBERT NLP Training Baseline
        meaning: maximum tokens per input sample (default 128)
      '326':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: tokens per input sequence / context window (default 512)
    cli_config_key: workload-specific
    interaction_precedence: do not confuse with prompt-only input_len
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  dataset_name:
    description: Dataset or prompt corpus identifier.
    data_type: string
    units: dataset identifier
    default_value: workload-specific
    valid_range: non-empty supported dataset identifier
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 121
    - 125
    - 221
    - 225
    - 227
    - 231
    - 321
    - 325
    workload_specific_overrides:
      '121':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: synthetic data generated directly on the GPU
      '125':
        workload_name: DistilBERT NLP Training Baseline
        meaning: GLUE SST-2 training dataset
      '221':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: training data source — synthetic or ImageNet ILSVRC2012
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: text classification dataset — imdb or sst2
      '227':
        workload_name: KV Cache Stress Test
        meaning: source of input prompts (synthetic)
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: name of dataset providing prompts (e.g. sharegpt, alpaca, gsm8k)
      '321':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: synthetic data generated directly on the GPU
      '325':
        workload_name: DistilBERT NLP Training Baseline
        meaning: GLUE SST-2 training dataset
    cli_config_key: workload-specific
    interaction_precedence: record canonical dataset and split when applicable
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  tensor_parallel_size:
    description: Number of GPUs across which model tensors are sharded.
    data_type: integer
    units: GPUs
    default_value: workload-specific
    valid_range: integer >= 1 and <= visible GPUs
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 128
    - 129
    - 228
    - 229
    - 230
    - 231
    - 328
    - 329
    workload_specific_overrides:
      '128':
        workload_name: vLLM Inference Throughput & Latency
        meaning: number of GPUs across which tensors are sharded
      '129':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: number of GPUs across which tensors are sharded
      '228':
        workload_name: vLLM Inference Throughput & Latency
        meaning: number of GPUs across which tensors are sharded
      '229':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: number of GPUs across which tensors are sharded
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: number of GPUs across which tensors are sharded
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: number of GPUs across which tensors are sharded
      '328':
        workload_name: vLLM Inference Throughput & Latency
        meaning: number of GPUs across which tensors are sharded
      '329':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: number of GPUs across which tensors are sharded
    cli_config_key: workload-specific
    interaction_precedence: must be compatible with num_gpus and backend support
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  K:
    description: GEMM K matrix dimension.
    data_type: integer
    units: matrix dimension
    default_value: workload-specific
    valid_range: integer > 0
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 119
    - 217
    - 219
    - 317
    - 319
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: 'matrix dimensions (A is M×K, B is K×N, C is M×N). transposeA, transposeB: whether to transpose A and/or
          B before multiply. alpha, beta: scalar multipliers for the product and accumulator. lda / ldb / ldc: leading dimension
          (stride in contiguous elements) for columns of A, B, C'
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: GEMM matrix dimensions — C(M×N) = A(M×K) × B(K×N)
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: 'matrix dimensions (A is M×K, B is K×N, C is M×N). transposeA, transposeB: whether to transpose A and/or
          B before multiply. alpha, beta: scalar multipliers for the product and accumulator. lda / ldb / ldc: leading dimension
          (stride in contiguous elements) for columns of A, B, C'
      '219':
        workload_name: TorchBench Microkernels
        meaning: GEMM matrix dimensions — C(M×N) = A(M×K) × B(K×N)
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: 'matrix dimensions (A is M×K, B is K×N, C is M×N). transposeA, transposeB: whether to transpose A and/or
          B before multiply. alpha, beta: scalar multipliers for the product and accumulator. lda / ldb / ldc: leading dimension
          (stride in contiguous elements) for columns of A, B, C'
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: GEMM matrix dimensions — C(M×N) = A(M×K) × B(K×N)
    cli_config_key: workload-specific
    interaction_precedence: M, N, and K jointly determine memory and compute cost
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  M:
    description: GEMM M matrix dimension.
    data_type: integer
    units: matrix dimension
    default_value: workload-specific
    valid_range: integer > 0
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 119
    - 217
    - 219
    - 317
    - 319
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: Defined by the workload description.
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: Defined by the workload description.
      '219':
        workload_name: TorchBench Microkernels
        meaning: Defined by the workload description.
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: M, N, and K jointly determine memory and compute cost
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  N:
    description: GEMM N matrix dimension.
    data_type: integer
    units: matrix dimension
    default_value: workload-specific
    valid_range: integer > 0
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 119
    - 217
    - 219
    - 317
    - 319
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: Defined by the workload description.
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: Defined by the workload description.
      '219':
        workload_name: TorchBench Microkernels
        meaning: Defined by the workload description.
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: M, N, and K jointly determine memory and compute cost
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  access_pattern:
    description: Memory traversal or access pattern.
    data_type: string
    units: pattern token
    default_value: workload-specific
    valid_range: supported pattern token
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 109
    - 110
    - 209
    - 210
    - 309
    - 310
    workload_specific_overrides:
      '109':
        workload_name: multichase Pointer-Chase
        meaning: ordering of pointer targets — sequential, random, etc
      '110':
        workload_name: NUMA Cache Latency Sweep
        meaning: memory traversal order — sequential, random, etc
      '209':
        workload_name: multichase Pointer-Chase
        meaning: ordering of pointer targets — sequential, random, etc
      '210':
        workload_name: NUMA Cache Latency Sweep
        meaning: memory traversal order — sequential, random, etc
      '309':
        workload_name: multichase Pointer-Chase
        meaning: ordering of pointer targets — sequential, random, etc
      '310':
        workload_name: NUMA Cache Latency Sweep
        meaning: memory traversal order — sequential, random, etc
    cli_config_key: workload-specific
    interaction_precedence: affects locality and measured bandwidth/latency
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  array_size:
    description: Number of elements in benchmark arrays.
    data_type: integer
    units: elements
    default_value: workload-specific
    valid_range: integer > 0 and memory-feasible
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 107
    - 115
    - 207
    - 215
    - 307
    - 315
    workload_specific_overrides:
      '107':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: number of elements in each STREAM array
      '115':
        workload_name: BabelStream HBM Bandwidth
        meaning: total elements in each of arrays A, B, C
      '207':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: number of elements in each STREAM array
      '215':
        workload_name: BabelStream HBM Bandwidth
        meaning: total elements in each of arrays A, B, C
      '307':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: number of elements in each STREAM array
      '315':
        workload_name: BabelStream HBM Bandwidth
        meaning: total elements in each of arrays A, B, C
    cli_config_key: workload-specific
    interaction_precedence: must fit target memory and may need block alignment
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  image_size:
    description: Spatial image dimensions.
    data_type: integer or tuple
    units: pixels
    default_value: workload-specific
    valid_range: positive supported dimensions
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 121
    - 122
    - 221
    - 222
    - 321
    - 322
    workload_specific_overrides:
      '121':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: spatial dimensions of generated input images (default 224×224)
      '122':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: spatial dimensions of input images (default 224×224)
      '221':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: spatial dimensions of input images (default 224×224)
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: spatial dimensions of input images (default 224×224)
      '321':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: spatial dimensions of generated input images (default 224×224)
      '322':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: spatial dimensions of input images (default 224×224)
    cli_config_key: workload-specific
    interaction_precedence: must match model and memory limits
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  learning_rate:
    description: Optimizer step size.
    data_type: number
    units: optimizer units
    default_value: workload-specific
    valid_range: number > 0; workload-specific range
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 121
    - 125
    - 221
    - 225
    - 321
    - 325
    workload_specific_overrides:
      '121':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: configurable, default 0.1 via --lr or config/benchmark_config.yaml
      '125':
        workload_name: DistilBERT NLP Training Baseline
        meaning: optimizer step size (default 5e-5)
      '221':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: optimizer step size (default 0.1)
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: optimizer step size (default 5e-5)
      '321':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: configurable, default 0.1 via --lr or config/benchmark_config.yaml
      '325':
        workload_name: DistilBERT NLP Training Baseline
        meaning: optimizer step size (default 5e-5)
    cli_config_key: workload-specific
    interaction_precedence: interacts with optimizer and gradient_accumulation
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  max_bytes:
    description: Maximum message or transfer size.
    data_type: integer
    units: bytes
    default_value: workload-specific
    valid_range: integer >= min_bytes
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 114
    - 116
    - 214
    - 216
    - 314
    - 316
    workload_specific_overrides:
      '114':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: maximum transfer size cap in bytes
      '116':
        workload_name: RCCL Bandwidth Test
        meaning: largest message size tested
      '214':
        workload_name: CUDA Memcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: maximum transfer size cap in bytes
      '216':
        workload_name: NCCL Bandwidth Test
        meaning: largest message size tested
      '314':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: maximum transfer size cap in bytes
      '316':
        workload_name: RCCL Bandwidth Test
        meaning: largest message size tested
    cli_config_key: workload-specific
    interaction_precedence: must be >= min_bytes and memory-feasible
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  max_num_seqs:
    description: Maximum sequences the inference engine processes concurrently.
    data_type: integer
    units: sequences
    default_value: workload-specific
    valid_range: integer >= 1
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 128
    - 129
    - 228
    - 229
    - 328
    - 329
    workload_specific_overrides:
      '128':
        workload_name: vLLM Inference Throughput & Latency
        meaning: maximum server-side sequences vLLM processes simultaneously
      '129':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: maximum server-side sequences vLLM processes simultaneously
      '228':
        workload_name: vLLM Inference Throughput & Latency
        meaning: maximum server-side sequences vLLM processes simultaneously
      '229':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: maximum server-side sequences vLLM processes simultaneously
      '328':
        workload_name: vLLM Inference Throughput & Latency
        meaning: maximum server-side sequences vLLM processes simultaneously
      '329':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: maximum server-side sequences vLLM processes simultaneously
    cli_config_key: workload-specific
    interaction_precedence: engine capacity; distinct from client concurrency
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  min_bytes:
    description: Minimum message or transfer size.
    data_type: integer
    units: bytes
    default_value: workload-specific
    valid_range: integer > 0
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 114
    - 116
    - 214
    - 216
    - 314
    - 316
    workload_specific_overrides:
      '114':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: initial / minimum transfer size in bytes
      '116':
        workload_name: RCCL Bandwidth Test
        meaning: smallest message size tested
      '214':
        workload_name: CUDA Memcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: initial / minimum transfer size in bytes
      '216':
        workload_name: NCCL Bandwidth Test
        meaning: smallest message size tested
      '314':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: initial / minimum transfer size in bytes
      '316':
        workload_name: RCCL Bandwidth Test
        meaning: smallest message size tested
    cli_config_key: workload-specific
    interaction_precedence: must be <= max_bytes
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  numa_node:
    description: NUMA node selected for placement.
    data_type: integer
    units: NUMA node
    default_value: workload-specific
    valid_range: integer >= 0 and present on host
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 107
    - 113
    - 207
    - 213
    - 307
    - 313
    workload_specific_overrides:
      '107':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: NUMA node ID on which memory is allocated (e.g. 0 = local)
      '113':
        workload_name: GUPS Random Memory
        meaning: NUMA domain to which memory is pinned (e.g. 0, 1)
      '207':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: NUMA node ID on which memory is allocated (e.g. 0 = local)
      '213':
        workload_name: GUPS Random Memory
        meaning: NUMA domain to which memory is pinned (e.g. 0, 1)
      '307':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: NUMA node ID on which memory is allocated (e.g. 0 = local)
      '313':
        workload_name: GUPS Random Memory
        meaning: NUMA domain to which memory is pinned (e.g. 0, 1)
    cli_config_key: workload-specific
    interaction_precedence: interacts with thread_affinity and memory placement
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  request_rate:
    description: Rate at which requests or prompts are submitted.
    data_type: number
    units: requests per second
    default_value: workload-specific
    valid_range: number >= 0
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 131
    - 228
    - 229
    - 230
    - 231
    - 331
    workload_specific_overrides:
      '131':
        workload_name: SGLang Serving Latency Benchmark
        meaning: arrival rate in requests/s (inf allowed for saturation)
      '228':
        workload_name: vLLM Inference Throughput & Latency
        meaning: requests submitted per second
      '229':
        workload_name: vLLM Token‑Generation Benchmark
        meaning: requests submitted per second
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: prompts submitted per second
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: prompts submitted per second
      '331':
        workload_name: SGLang Serving Latency Benchmark
        meaning: arrival rate in requests/s (inf allowed for saturation)
    cli_config_key: workload-specific
    interaction_precedence: may be ignored or overridden when closed-loop concurrency is used
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  step_factor:
    description: Multiplier between successive sweep sizes.
    data_type: number
    units: multiplicative factor
    default_value: workload-specific
    valid_range: number > 1
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 114
    - 116
    - 214
    - 216
    - 314
    - 316
    workload_specific_overrides:
      '114':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: multiplier growing transfer_size each iteration (default 2 → 1K, 2K, 4K…)
      '116':
        workload_name: RCCL Bandwidth Test
        meaning: multiplier between successive message sizes (default 2 → 1KB, 2KB, 4KB…)
      '214':
        workload_name: CUDA Memcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: multiplier growing transfer_size each iteration (default 2 → 1K, 2K, 4K…)
      '216':
        workload_name: NCCL Bandwidth Test
        meaning: multiplier between successive message sizes (default 2 → 1KB, 2KB, 4KB…)
      '314':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: multiplier growing transfer_size each iteration (default 2 → 1K, 2K, 4K…)
      '316':
        workload_name: RCCL Bandwidth Test
        meaning: multiplier between successive message sizes (default 2 → 1KB, 2KB, 4KB…)
    cli_config_key: workload-specific
    interaction_precedence: controls logarithmic sweep progression
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  stride:
    description: Distance between successive memory accesses.
    data_type: integer
    units: workload-specific units
    default_value: workload-specific
    valid_range: positive workload-specific value
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 109
    - 110
    - 209
    - 210
    - 309
    - 310
    workload_specific_overrides:
      '109':
        workload_name: multichase Pointer-Chase
        meaning: distance in bytes between consecutive pointer addresses (default 64)
      '110':
        workload_name: NUMA Cache Latency Sweep
        meaning: bytes between successive memory accesses (e.g. 64, 128, 4096)
      '209':
        workload_name: multichase Pointer-Chase
        meaning: distance in bytes between consecutive pointer addresses (default 64)
      '210':
        workload_name: NUMA Cache Latency Sweep
        meaning: bytes between successive memory accesses (e.g. 64, 128, 4096)
      '309':
        workload_name: multichase Pointer-Chase
        meaning: distance in bytes between consecutive pointer addresses (default 64)
      '310':
        workload_name: NUMA Cache Latency Sweep
        meaning: bytes between successive memory accesses (e.g. 64, 128, 4096)
    cli_config_key: workload-specific
    interaction_precedence: units must be documented as bytes, elements, or tokens
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  working_set_size:
    description: Memory region size used by the workload.
    data_type: integer or string
    units: bytes or size token
    default_value: workload-specific
    valid_range: positive memory-feasible value
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 109
    - 110
    - 209
    - 210
    - 309
    - 310
    workload_specific_overrides:
      '109':
        workload_name: multichase Pointer-Chase
        meaning: memory region per thread in bytes (default 256 MB)
      '110':
        workload_name: NUMA Cache Latency Sweep
        meaning: memory footprint on the target node (e.g. 32K, 4MB, 1GB)
      '209':
        workload_name: multichase Pointer-Chase
        meaning: memory region per thread in bytes (default 256 MB)
      '210':
        workload_name: NUMA Cache Latency Sweep
        meaning: memory footprint on the target node (e.g. 32K, 4MB, 1GB)
      '309':
        workload_name: multichase Pointer-Chase
        meaning: memory region per thread in bytes (default 256 MB)
      '310':
        workload_name: NUMA Cache Latency Sweep
        meaning: memory footprint on the target node (e.g. 32K, 4MB, 1GB)
    cli_config_key: workload-specific
    interaction_precedence: must fit target memory and may interact with page_size and stride
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  max_total_tokens:
    description: Maximum server-side tokens held in the scheduling or KV-cache pool.
    data_type: integer
    units: tokens
    default_value: workload-specific
    valid_range: integer >= 1
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 131
    - 230
    - 231
    - 331
    workload_specific_overrides:
      '131':
        workload_name: SGLang Serving Latency Benchmark
        meaning: server KV-cache token capacity
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: maximum server-side tokens held in the KV cache pool
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: maximum server-side tokens held in the KV cache pool
      '331':
        workload_name: SGLang Serving Latency Benchmark
        meaning: server KV-cache token capacity
    cli_config_key: workload-specific
    interaction_precedence: must accommodate configured input/output lengths
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_workers:
    description: Worker processes or data-loader/service workers.
    data_type: integer
    units: workers
    default_value: workload-specific
    valid_range: integer >= 0
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 122
    - 221
    - 222
    - 322
    workload_specific_overrides:
      '122':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: CPU processes feeding the PyTorch DataLoader
      '221':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: CPU processes feeding the PyTorch DataLoader
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: CPU processes feeding the PyTorch DataLoader
      '322':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: CPU processes feeding the PyTorch DataLoader
    cli_config_key: workload-specific
    interaction_precedence: distinct from execution threads
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  optimizer:
    description: Optimization algorithm used for training.
    data_type: string
    units: optimizer token
    default_value: workload-specific
    valid_range: supported optimizer token
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 125
    - 221
    - 225
    - 325
    workload_specific_overrides:
      '125':
        workload_name: DistilBERT NLP Training Baseline
        meaning: fixed AdamW optimizer
      '221':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: weight update algorithm — SGD, AdamW, etc
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: weight update algorithm — AdamW, SGD, etc
      '325':
        workload_name: DistilBERT NLP Training Baseline
        meaning: fixed AdamW optimizer
    cli_config_key: workload-specific
    interaction_precedence: interacts with learning_rate and gradient_accumulation
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  alpha:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 217
    - 317
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: Defined by the workload description.
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  atol:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 105
    - 205
    - 305
    workload_specific_overrides:
      '105':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: absolute tolerance for the same comparison (e.g. 1e-6 for FP32)
      '205':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: absolute tolerance for the same comparison (e.g. 1e-6 for FP32)
      '305':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: absolute tolerance for the same comparison (e.g. 1e-6 for FP32)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  bandwidth_limit:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 108
    - 208
    - 308
    workload_specific_overrides:
      '108':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '208':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '308':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  batch_count:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 217
    - 317
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: independent GEMM equations executed per batch call
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: independent GEMM equations executed per batch call
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: independent GEMM equations executed per batch call
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  beta:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 217
    - 317
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: 'scalar multipliers for the product and accumulator. lda / ldb / ldc: leading dimension (stride in contiguous
          elements) for columns of A, B, C'
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: 'scalar multipliers for the product and accumulator. lda / ldb / ldc: leading dimension (stride in contiguous
          elements) for columns of A, B, C'
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: 'scalar multipliers for the product and accumulator. lda / ldb / ldc: leading dimension (stride in contiguous
          elements) for columns of A, B, C'
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  buffer_length:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 108
    - 208
    - 308
    workload_specific_overrides:
      '108':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '208':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '308':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  bw_mem:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 112
    - 212
    - 312
    workload_specific_overrides:
      '112':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for memory read/write/copy bandwidth (msg_size=64, iterations=500)
      '212':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for memory read/write/copy bandwidth (msg_size=64, iterations=500)
      '312':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for memory read/write/copy bandwidth (msg_size=64, iterations=500)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  bw_pipe:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 112
    - 212
    - 312
    workload_specific_overrides:
      '112':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for Unix pipe IPC throughput (msg_size=64, iterations=500)
      '212':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for Unix pipe IPC throughput (msg_size=64, iterations=500)
      '312':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for Unix pipe IPC throughput (msg_size=64, iterations=500)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  cache_level:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 110
    - 210
    - 310
    workload_specific_overrides:
      '110':
        workload_name: NUMA Cache Latency Sweep
        meaning: target tier of the cache hierarchy — L1, L2, L3, DRAM, etc
      '210':
        workload_name: NUMA Cache Latency Sweep
        meaning: target tier of the cache hierarchy — L1, L2, L3, DRAM, etc
      '310':
        workload_name: NUMA Cache Latency Sweep
        meaning: target tier of the cache hierarchy — L1, L2, L3, DRAM, etc
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  chunk_overlap:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 132
    - 232
    - 332
    workload_specific_overrides:
      '132':
        workload_name: RAG Pipeline Sweep
        meaning: tokens shared between consecutive chunks (default 32)
      '232':
        workload_name: RAG Pipeline Sweep
        meaning: tokens shared between consecutive chunks (default 32)
      '332':
        workload_name: RAG Pipeline Sweep
        meaning: tokens shared between consecutive chunks (default 32)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  chunk_size:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 132
    - 232
    - 332
    workload_specific_overrides:
      '132':
        workload_name: RAG Pipeline Sweep
        meaning: tokens per document chunk (default 256)
      '232':
        workload_name: RAG Pipeline Sweep
        meaning: tokens per document chunk (default 256)
      '332':
        workload_name: RAG Pipeline Sweep
        meaning: tokens per document chunk (default 256)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  clock_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 102
    - 202
    - 302
    workload_specific_overrides:
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: reads current GPU core and memory clock values (default true)
      '202':
        workload_name: NVIDIA System Validation (clocks, temperature, etc.)
        meaning: reads SM, graphics, and memory clocks
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: reads current GPU core and memory clock values (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  collective:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 116
    - 216
    - 316
    workload_specific_overrides:
      '116':
        workload_name: RCCL Bandwidth Test
        meaning: operation type — all_reduce, all_gather, broadcast, reduce, or reduce_scatter
      '216':
        workload_name: NCCL Bandwidth Test
        meaning: operation type — all_reduce, all_gather, broadcast, reduce, reduce_scatter, or alltoall
      '316':
        workload_name: RCCL Bandwidth Test
        meaning: operation type — all_reduce, all_gather, broadcast, reduce, or reduce_scatter
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  concurrency:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 127
    - 227
    - 327
    workload_specific_overrides:
      '127':
        workload_name: KV Cache Stress Test
        meaning: maximum client-side requests in flight simultaneously (default 8)
      '227':
        workload_name: KV Cache Stress Test
        meaning: maximum client-side requests in flight simultaneously (default 8)
      '327':
        workload_name: KV Cache Stress Test
        meaning: maximum client-side requests in flight simultaneously (default 8)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  conv_direction:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: fwd (forward pass) and/or bwd_d (backward data / input gradient)
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: forward, backward-data, and/or backward-filter. N/C/H/W/K/R/S, strides, padding, dilation, and groups define
          the problem
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: fwd (forward pass) and/or bwd_d (backward data / input gradient)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  count:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 104
    - 204
    - 304
    workload_specific_overrides:
      '104':
        workload_name: Silent Data Corruption (SDC)
        meaning: number of pattern/write-read-verify cycles to execute per iteration (default 1)
      '204':
        workload_name: Silent Data Corruption (SDC)
        meaning: verify cycles per iteration
      '304':
        workload_name: Silent Data Corruption (SDC)
        meaning: number of pattern/write-read-verify cycles to execute per iteration (default 1)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  cpu_load_percent:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 103
    - 203
    - 303
    workload_specific_overrides:
      '103':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: target CPU utilization % (default 50)
      '203':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: target CPU utilization % (default 50)
      '303':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: target CPU utilization % (default 50)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  direct:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: bypass OS page cache if set to 1
      '206':
        workload_name: FIO NVMe Sweep
        meaning: bypass OS page cache if set to 1
      '306':
        workload_name: FIO NVMe Sweep
        meaning: bypass OS page cache if set to 1
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  direction:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 114
    - 214
    - 314
    workload_specific_overrides:
      '114':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: copy direction — H2D (host-to-device), D2H (device-to-host), or D2D (device-to-device)
      '214':
        workload_name: CUDA Memcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: copy direction — H2D (host-to-device), D2H (device-to-host), or D2D (device-to-device)
      '314':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: copy direction — H2D (host-to-device), D2H (device-to-host), or D2D (device-to-device)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  dmesg_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 102
    - 202
    - 302
    workload_specific_overrides:
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: scans kernel ring buffer for genuine GPU faults and driver errors, excluding known benign capability notices
          such as “SDMA reset capability is not supported” (default true)
      '202':
        workload_name: NVIDIA System Validation (clocks, temperature, etc.)
        meaning: scans kernel logs for Xid and driver faults
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: scans kernel ring buffer for genuine GPU faults and driver errors, excluding known benign capability notices
          such as “SDMA reset capability is not supported” (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  event_list:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 111
    - 211
    - 311
    workload_specific_overrides:
      '111':
        workload_name: Linux perf PMU Analysis Harness
        meaning: hardware counters to collect — cycles, instructions, branches, branch-misses, cache-references, cache-misses,
          L1-dcache-loads, L1-dcache-load-misses, LLC-loads, LLC-load-misses, dTLB-load-misses, iTLB-load-misses, stalled-cycles-frontend,
          stalled-cycles-backend
      '211':
        workload_name: Linux perf PMU Analysis Harness
        meaning: hardware counters to collect — cycles, instructions, branches, branch-misses, cache-references, cache-misses,
          L1-dcache-loads, L1-dcache-load-misses, LLC-loads, LLC-load-misses, dTLB-load-misses, iTLB-load-misses, stalled-cycles-frontend,
          stalled-cycles-backend
      '311':
        workload_name: Linux perf PMU Analysis Harness
        meaning: hardware counters to collect — cycles, instructions, branches, branch-misses, cache-references, cache-misses,
          L1-dcache-loads, L1-dcache-load-misses, LLC-loads, LLC-load-misses, dTLB-load-misses, iTLB-load-misses, stalled-cycles-frontend,
          stalled-cycles-backend
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  filename:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: target device or file path for I/O
      '206':
        workload_name: FIO NVMe Sweep
        meaning: target device or file path for I/O
      '306':
        workload_name: FIO NVMe Sweep
        meaning: target device or file path for I/O
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  firmware_version_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 102
    - 202
    - 302
    workload_specific_overrides:
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: validates GPU firmware revision against expected values (default true)
      '202':
        workload_name: NVIDIA System Validation (clocks, temperature, etc.)
        meaning: validates VBIOS/InfoROM
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: validates GPU firmware revision against expected values (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  gpu_load_percent:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 103
    - 203
    - 303
    workload_specific_overrides:
      '103':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: target GPU duty cycle enforced by recurring rocblas-bench GEMM operations (default 50%)
      '203':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: target GPU utilization % (default 50)
      '303':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: target GPU duty cycle enforced by recurring rocblas-bench GEMM operations (default 50%)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  group_count:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: channel grouping (current execution uses 1; grouped/depthwise coverage is not included)
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: channel grouping (current execution uses 1; grouped/depthwise coverage is not included)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  guidance_scale:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 124
    - 224
    - 324
    workload_specific_overrides:
      '124':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: Classifier-Free Guidance strength (e.g. 0.0, 5.0)
      '224':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: Classifier-Free Guidance strength (e.g. 0.0, 5.0)
      '324':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: Classifier-Free Guidance strength (e.g. 0.0, 5.0)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  hidden_size:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 126
    - 226
    - 326
    workload_specific_overrides:
      '126':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: neuron width of each hidden layer (default 128)
      '226':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: neuron width of each hidden layer (default 128)
      '326':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: neuron width of each hidden layer (default 128)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  image_resolution:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 124
    - 224
    - 324
    workload_specific_overrides:
      '124':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: output image dimensions (default 1024×1024)
      '224':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: output image dimensions (default 1024×1024)
      '324':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: output image dimensions (default 1024×1024)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  image_source:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 122
    - 222
    - 322
    workload_specific_overrides:
      '122':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: data source — synthetic or ImageNet ILSVRC2012
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: data source — synthetic or ImageNet ILSVRC2012
      '322':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: data source — synthetic or ImageNet ILSVRC2012
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  input_channels:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: 'C — input feature maps/color channels (e.g. 3 for RGB). input_height / input_width: H×W spatial dimensions
          (default 224×224)'
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: 'C — input feature maps/color channels (e.g. 3 for RGB). input_height / input_width: H×W spatial dimensions
          (default 224×224)'
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  input_height:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: Defined by the workload description.
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  input_shape:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 119
    - 219
    - 319
    workload_specific_overrides:
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: 'Conv2D input tensor shape — (BatchSize, Channels, Height, Width), e.g. (1,3,224,224). M / N / K: GEMM matrix
          dimensions — C(M×N) = A(M×K) × B(K×N)'
      '219':
        workload_name: TorchBench Microkernels
        meaning: 'Conv2D input tensor shape — (BatchSize, Channels, Height, Width), e.g. (1,3,224,224). M / N / K: GEMM matrix
          dimensions — C(M×N) = A(M×K) × B(K×N)'
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: 'Conv2D input tensor shape — (BatchSize, Channels, Height, Width), e.g. (1,3,224,224). M / N / K: GEMM matrix
          dimensions — C(M×N) = A(M×K) × B(K×N)'
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  input_source:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: data source — synthetic (randomly generated tensors)
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: synthetic tensors. batch_size, input_channels, input_height, input_width, output_channels, kernel_height,
          kernel_width, stride_h, stride_w, group_count, and search_strategy define the cuDNN problem
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: data source — synthetic (randomly generated tensors)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  input_width:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: H×W spatial dimensions (default 224×224)
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: H×W spatial dimensions (default 224×224)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  interval:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 108
    - 208
    - 308
    workload_specific_overrides:
      '108':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '208':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '308':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  io_depth:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: I/O requests simultaneously in flight per job
      '206':
        workload_name: FIO NVMe Sweep
        meaning: I/O requests simultaneously in flight per job
      '306':
        workload_name: FIO NVMe Sweep
        meaning: I/O requests simultaneously in flight per job
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  io_engine:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: kernel I/O submission backend (e.g. libaio)
      '206':
        workload_name: FIO NVMe Sweep
        meaning: kernel I/O submission backend (e.g. libaio)
      '306':
        workload_name: FIO NVMe Sweep
        meaning: kernel I/O submission backend (e.g. libaio)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  kernel_height:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: Defined by the workload description.
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  kernel_type:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 119
    - 219
    - 319
    workload_specific_overrides:
      '119':
        workload_name: PyTorch Microkernel Suite
        meaning: operation to benchmark — conv2d or gemm
      '219':
        workload_name: TorchBench Microkernels
        meaning: operation to benchmark — conv2d or gemm
      '319':
        workload_name: PyTorch Microkernel Suite
        meaning: operation to benchmark — conv2d or gemm
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  kernel_types:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 107
    - 207
    - 307
    workload_specific_overrides:
      '107':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: subset of kernels to run — copy, scale, add, triad
      '207':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: subset of kernels to run — copy, scale, add, triad
      '307':
        workload_name: STREAM - CPU (Copy/Scale/Add/Triad)
        meaning: subset of kernels to run — copy, scale, add, triad
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  kernel_version:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 101
    - 201
    - 301
    workload_specific_overrides:
      '101':
        workload_name: System Config Verification
        meaning: expected Linux kernel version string (e.g. 6.8.0-134-generic)
      '201':
        workload_name: System Config Verification
        meaning: expected Linux kernel version
      '301':
        workload_name: System Config Verification
        meaning: expected Linux kernel version string (e.g. 7.0.0-27-generic)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  kernel_width:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: 'filter spatial size (default 3×3). stride_h / stride_w: step between filter applications (default 1×1)'
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: 'filter spatial size (default 3×3). stride_h / stride_w: step between filter applications (default 1×1)'
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  kernels:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 115
    - 215
    - 315
    workload_specific_overrides:
      '115':
        workload_name: BabelStream HBM Bandwidth
        meaning: subset to run — C/M/A/T/D
      '215':
        workload_name: BabelStream HBM Bandwidth
        meaning: subset to run — C/M/A/T/D
      '315':
        workload_name: BabelStream HBM Bandwidth
        meaning: subset to run — C/M/A/T/D
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  kv_cache_dtype:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 127
    - 227
    - 327
    workload_specific_overrides:
      '127':
        workload_name: KV Cache Stress Test
        meaning: precision for KV cache key and value storage (default BF16)
      '227':
        workload_name: KV Cache Stress Test
        meaning: precision for KV cache key and value storage (default BF16)
      '327':
        workload_name: KV Cache Stress Test
        meaning: precision for KV cache key and value storage (default BF16)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  lat_ctx:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 112
    - 212
    - 312
    workload_specific_overrides:
      '112':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for context-switch latency (msg_size=64, iterations=500)
      '212':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for context-switch latency (msg_size=64, iterations=500)
      '312':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for context-switch latency (msg_size=64, iterations=500)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  lat_mem_rd:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 112
    - 212
    - 312
    workload_specific_overrides:
      '112':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for cache and DRAM read latency (stride=64)
      '212':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for cache and DRAM read latency (stride=64)
      '312':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for cache and DRAM read latency (stride=64)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  lat_syscall:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 112
    - 212
    - 312
    workload_specific_overrides:
      '112':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for OS syscall latency (iterations=500)
      '212':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for OS syscall latency (iterations=500)
      '312':
        workload_name: lmbench Microbench Suite
        meaning: sub-benchmark for OS syscall latency (iterations=500)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  lda:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 217
    - 317
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: Defined by the workload description.
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  ldb:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 217
    - 317
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: Defined by the workload description.
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  ldc:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 217
    - 317
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: leading dimension (stride in contiguous elements) for columns of A, B, C
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: leading dimension (stride in contiguous elements) for columns of A, B, C
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: leading dimension (stride in contiguous elements) for columns of A, B, C
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  max_model_len:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 127
    - 227
    - 327
    workload_specific_overrides:
      '127':
        workload_name: KV Cache Stress Test
        meaning: maximum context window in tokens — prompt + output (default 8192)
      '227':
        workload_name: KV Cache Stress Test
        meaning: maximum context window in tokens — prompt + output (default 8192)
      '327':
        workload_name: KV Cache Stress Test
        meaning: maximum context window in tokens — prompt + output (default 8192)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  memory_size:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 104
    - 204
    - 304
    workload_specific_overrides:
      '104':
        workload_name: Silent Data Corruption (SDC)
        meaning: HBM region tested (default 1 GB; not total installed HBM)
      '204':
        workload_name: Silent Data Corruption (SDC)
        meaning: HBM region tested
      '304':
        workload_name: Silent Data Corruption (SDC)
        meaning: HBM region tested (default 1 GB; not total installed HBM)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  norm_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 217
    - 317
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: whether rocblas-bench computes and reports error norms
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: whether custom cuBLAS harness computes and reports error norms
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: whether rocblas-bench computes and reports error norms
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_cpus:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 111
    - 211
    - 311
    workload_specific_overrides:
      '111':
        workload_name: Linux perf PMU Analysis Harness
        meaning: number of CPUs to pin and monitor (default 1)
      '211':
        workload_name: Linux perf PMU Analysis Harness
        meaning: number of CPUs to pin and monitor (default 1)
      '311':
        workload_name: Linux perf PMU Analysis Harness
        meaning: number of CPUs to pin and monitor (default 1)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_heads:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 126
    - 226
    - 326
    workload_specific_overrides:
      '126':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: attention heads per layer (default 8)
      '226':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: attention heads per layer (default 8)
      '326':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: attention heads per layer (default 8)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_images:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 124
    - 224
    - 324
    workload_specific_overrides:
      '124':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: total images generated across the entire benchmark run
      '224':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: total images generated across the entire benchmark run
      '324':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: total images generated across the entire benchmark run
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_inference_steps:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 124
    - 224
    - 324
    workload_specific_overrides:
      '124':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: denoising diffusion steps per image
      '224':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: denoising diffusion steps per image
      '324':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: denoising diffusion steps per image
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_jobs:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: parallel FIO job threads (default 4)
      '206':
        workload_name: FIO NVMe Sweep
        meaning: parallel FIO job threads (default 4)
      '306':
        workload_name: FIO NVMe Sweep
        meaning: parallel FIO job threads (default 4)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_layers:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 126
    - 226
    - 326
    workload_specific_overrides:
      '126':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: number of hidden transformer layers, excluding embedding and unembedding (default 4)
      '226':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: number of hidden transformer layers, excluding embedding and unembedding (default 4)
      '326':
        workload_name: JAX XLA Transformer Forward Pass
        meaning: number of hidden transformer layers, excluding embedding and unembedding (default 4)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_numa_nodes:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 109
    - 209
    - 309
    workload_specific_overrides:
      '109':
        workload_name: multichase Pointer-Chase
        meaning: NUMA nodes used (1 = local)
      '209':
        workload_name: multichase Pointer-Chase
        meaning: NUMA nodes used (1 = local)
      '309':
        workload_name: multichase Pointer-Chase
        meaning: NUMA nodes used (1 = local)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  num_updates:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 113
    - 213
    - 313
    workload_specific_overrides:
      '113':
        workload_name: GUPS Random Memory
        meaning: total random read-modify-write operations executed
      '213':
        workload_name: GUPS Random Memory
        meaning: total random read-modify-write operations executed
      '313':
        workload_name: GUPS Random Memory
        meaning: total random read-modify-write operations executed
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  omit_initial_seconds:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 108
    - 208
    - 308
    workload_specific_overrides:
      '108':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '208':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '308':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  op_name:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 105
    - 205
    - 305
    workload_specific_overrides:
      '105':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: operation to test — conv2d or matmul
      '205':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: operation to test — conv2d or matmul
      '305':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: operation to test — conv2d or matmul
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  output_channels:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: 'number of filters / output feature maps (default 64). kernel_height / kernel_width: filter spatial size
          (default 3×3). stride_h / stride_w: step between filter applications (default 1×1)'
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: 'number of filters / output feature maps (default 64). kernel_height / kernel_width: filter spatial size
          (default 3×3). stride_h / stride_w: step between filter applications (default 1×1)'
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  panel_factorization:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 120
    - 220
    - 320
    workload_specific_overrides:
      '120':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: LU algorithm (e.g. "2" = right-looking)
      '220':
        workload_name: NVIDIA HPL FP64 (High Performance Linpack)
        meaning: LU algorithm (e.g. "2" = right-looking)
      '320':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: LU algorithm (e.g. "2" = right-looking)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  parallel_jobs:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 112
    - 212
    - 312
    workload_specific_overrides:
      '112':
        workload_name: lmbench Microbench Suite
        meaning: concurrent benchmark instances (default 2)
      '212':
        workload_name: lmbench Microbench Suite
        meaning: concurrent benchmark instances (default 2)
      '312':
        workload_name: lmbench Microbench Suite
        meaning: concurrent benchmark instances (default 2)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  parallel_streams:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 108
    - 208
    - 308
    workload_specific_overrides:
      '108':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '208':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '308':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  pattern_type:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 104
    - 204
    - 304
    workload_specific_overrides:
      '104':
        workload_name: Silent Data Corruption (SDC)
        meaning: write/read pattern — walking-1s (single bit walks across all bit positions); also supports walking-0s, checkerboard,
          etc
      '204':
        workload_name: Silent Data Corruption (SDC)
        meaning: walking-1s, walking-0s, checkerboard, moving inversions, or random
      '304':
        workload_name: Silent Data Corruption (SDC)
        meaning: write/read pattern — walking-1s (single bit walks across all bit positions); also supports walking-0s, checkerboard,
          etc
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  pcie_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 102
    - 202
    - 302
    workload_specific_overrides:
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: reads PCIe link generation and width for each GPU (default true)
      '202':
        workload_name: NVIDIA System Validation (clocks, temperature, etc.)
        meaning: reads PCIe generation and width
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: reads PCIe link generation and width for each GPU (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  permissions_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 101
    - 201
    - 301
    workload_specific_overrides:
      '101':
        workload_name: System Config Verification
        meaning: verifies correct user/group permissions for GPU device access (default true)
      '201':
        workload_name: System Config Verification
        meaning: verifies access to /dev/nvidia* devices
      '301':
        workload_name: System Config Verification
        meaning: verifies correct user/group permissions for GPU device access (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  pinned_memory:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 114
    - 214
    - 314
    workload_specific_overrides:
      '114':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: use page-locked (pinned) host memory vs. OS-paged memory (default true)
      '214':
        workload_name: CUDA Memcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: use page-locked (pinned) host memory vs. OS-paged memory (default true)
      '314':
        workload_name: hipMemcpy Bandwidth Test (H2D, D2H, D2D)
        meaning: use page-locked (pinned) host memory vs. OS-paged memory (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  port:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 108
    - 208
    - 308
    workload_specific_overrides:
      '108':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '208':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '308':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  power_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 102
    - 202
    - 302
    workload_specific_overrides:
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: captures GPU power draw telemetry (default true)
      '202':
        workload_name: NVIDIA System Validation (clocks, temperature, etc.)
        meaning: captures board power draw
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: captures GPU power draw telemetry (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  power_threshold:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 103
    - 203
    - 303
    workload_specific_overrides:
      '103':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: maximum allowable system power draw in Watts (default 500 W)
      '203':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: maximum allowable system power draw in Watts (default 500 W)
      '303':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: maximum allowable system power draw in Watts (default 500 W)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  prefetch_enabled:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 109
    - 209
    - 309
    workload_specific_overrides:
      '109':
        workload_name: multichase Pointer-Chase
        meaning: enable hardware prefetcher for future addresses (default false)
      '209':
        workload_name: multichase Pointer-Chase
        meaning: enable hardware prefetcher for future addresses (default false)
      '309':
        workload_name: multichase Pointer-Chase
        meaning: enable hardware prefetcher for future addresses (default false)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  problem_size_N:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 120
    - 220
    - 320
    workload_specific_overrides:
      '120':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: side length of square matrix A (e.g. 32768, 65536); total FLOPs ≈ (2/3)N³
      '220':
        workload_name: NVIDIA HPL FP64 (High Performance Linpack)
        meaning: side length of square matrix A (e.g. 32768, 65536); total FLOPs ≈ (2/3)N³
      '320':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: side length of square matrix A (e.g. 32768, 65536); total FLOPs ≈ (2/3)N³
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  process_grid_P:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 120
    - 220
    - 320
    workload_specific_overrides:
      '120':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: rows in the P×Q GPU process grid
      '220':
        workload_name: NVIDIA HPL FP64 (High Performance Linpack)
        meaning: rows in the P×Q GPU process grid
      '320':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: rows in the P×Q GPU process grid
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  process_grid_Q:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 120
    - 220
    - 320
    workload_specific_overrides:
      '120':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: columns in the P×Q GPU process grid; P×Q = total GPUs
      '220':
        workload_name: NVIDIA HPL FP64 (High Performance Linpack)
        meaning: columns in the P×Q GPU process grid; P×Q = total GPUs
      '320':
        workload_name: rocHPL FP64 (High Performance Linpack)
        meaning: columns in the P×Q GPU process grid; P×Q = total GPUs
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  protocol:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 108
    - 208
    - 308
    workload_specific_overrides:
      '108':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '208':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '308':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  query_count:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 132
    - 232
    - 332
    workload_specific_overrides:
      '132':
        workload_name: RAG Pipeline Sweep
        meaning: profile-dependent—smoke 2, baseline 100, extended 250. Embedding, LLM, and reranker model IDs are fixed identity
          (Software Framework), not sweep parameters
      '232':
        workload_name: RAG Pipeline Sweep
        meaning: total queries executed during the run (default 100)
      '332':
        workload_name: RAG Pipeline Sweep
        meaning: profile-dependent—smoke 2, baseline 100, extended 250. Embedding, LLM, and reranker model IDs are fixed identity
          (Software Framework), not sweep parameters
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  query_length:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 132
    - 232
    - 332
    workload_specific_overrides:
      '132':
        workload_name: RAG Pipeline Sweep
        meaning: maximum tokens per input query (default 32)
      '232':
        workload_name: RAG Pipeline Sweep
        meaning: tokens per input query (default 32)
      '332':
        workload_name: RAG Pipeline Sweep
        meaning: maximum tokens per input query (default 32)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  repetitions:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 112
    - 212
    - 312
    workload_specific_overrides:
      '112':
        workload_name: lmbench Microbench Suite
        meaning: number of times the full workload suite is repeated (default 10)
      '212':
        workload_name: lmbench Microbench Suite
        meaning: number of times the full workload suite is repeated (default 10)
      '312':
        workload_name: lmbench Microbench Suite
        meaning: number of times the full workload suite is repeated (default 10)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  required_packages:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 101
    - 201
    - 301
    workload_specific_overrides:
      '101':
        workload_name: System Config Verification
        meaning: comma-separated list of packages that must be installed (e.g. rocm-core, rocm-smi)
      '201':
        workload_name: System Config Verification
        meaning: required NVIDIA driver, CUDA toolkit, DCGM, and NCCL packages
      '301':
        workload_name: System Config Verification
        meaning: comma-separated list of packages that must be installed (e.g. rocm-core, rocm-smi)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  reverse_mode:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 108
    - 208
    - 308
    workload_specific_overrides:
      '108':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '208':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '308':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  rtol:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 105
    - 205
    - 305
    workload_specific_overrides:
      '105':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: relative tolerance for comparing GPU output to a CPU FP32 reference (e.g. 1e-5 for FP32)
      '205':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: relative tolerance for comparing GPU output to a CPU FP64 reference (e.g. 1e-5 for FP32)
      '305':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: relative tolerance for comparing GPU output to a CPU FP32 reference (e.g. 1e-5 for FP32)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  runtime:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: total workload duration in seconds (default 300)
      '206':
        workload_name: FIO NVMe Sweep
        meaning: total workload duration in seconds (default 300)
      '306':
        workload_name: FIO NVMe Sweep
        meaning: total workload duration in seconds (default 300)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  rw:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: access pattern — randread, write, randwrite, read, etc
      '206':
        workload_name: FIO NVMe Sweep
        meaning: access pattern — randread, write, randwrite, read, etc
      '306':
        workload_name: FIO NVMe Sweep
        meaning: access pattern — randread, write, randwrite, read, etc
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  scheduler:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 124
    - 224
    - 324
    workload_specific_overrides:
      '124':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: denoising scheduler algorithm (default Euler)
      '224':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: denoising scheduler algorithm (default Euler)
      '324':
        workload_name: Stable Diffusion XL Inference Baseline
        meaning: denoising scheduler algorithm (default Euler)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  search_strategy:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: solver selection — "find" exhaustively benchmarks available kernels
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: solver selection — "find" exhaustively benchmarks available kernels
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  shape:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 105
    - 205
    - 305
    workload_specific_overrides:
      '105':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: input tensor dimensions, e.g. (1,) or (8,3,224,224)
      '205':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: input tensor dimensions, e.g. (1,) or (8,3,224,224)
      '305':
        workload_name: PyTorch Tensor Op Correctness Suite
        meaning: input tensor dimensions, e.g. (1,) or (8,3,224,224)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  size:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: total data size per FIO job
      '206':
        workload_name: FIO NVMe Sweep
        meaning: total data size per FIO job
      '306':
        workload_name: FIO NVMe Sweep
        meaning: total data size per FIO job
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  source_numa_node:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 110
    - 210
    - 310
    workload_specific_overrides:
      '110':
        workload_name: NUMA Cache Latency Sweep
        meaning: NUMA node on which threads execute (e.g. 0, 1, 2)
      '210':
        workload_name: NUMA Cache Latency Sweep
        meaning: NUMA node on which threads execute (e.g. 0, 1, 2)
      '310':
        workload_name: NUMA Cache Latency Sweep
        meaning: NUMA node on which threads execute (e.g. 0, 1, 2)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  stonewall:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: wait for preceding jobs to finish before starting next job group
      '206':
        workload_name: FIO NVMe Sweep
        meaning: wait for preceding jobs to finish before starting next job group
      '306':
        workload_name: FIO NVMe Sweep
        meaning: wait for preceding jobs to finish before starting next job group
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  stride_h:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: Defined by the workload description.
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  stride_w:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 118
    - 218
    - 318
    workload_specific_overrides:
      '118':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: step between filter applications (default 1×1)
      '218':
        workload_name: cuDNN Convolution Microbenchmark
        meaning: Defined by the workload description.
      '318':
        workload_name: MIOpen Convolution Solver Microbenchmark
        meaning: step between filter applications (default 1×1)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  sync_mode:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: serialize I/O (sync) vs. allow simultaneous operations (async)
      '206':
        workload_name: FIO NVMe Sweep
        meaning: serialize I/O (sync) vs. allow simultaneous operations (async)
      '306':
        workload_name: FIO NVMe Sweep
        meaning: serialize I/O (sync) vs. allow simultaneous operations (async)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  table_size:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 113
    - 213
    - 313
    workload_specific_overrides:
      '113':
        workload_name: GUPS Random Memory
        meaning: size of the memory region under test in bytes
      '213':
        workload_name: GUPS Random Memory
        meaning: size of the memory region under test in bytes
      '313':
        workload_name: GUPS Random Memory
        meaning: size of the memory region under test in bytes
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  target_numa_node:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 110
    - 210
    - 310
    workload_specific_overrides:
      '110':
        workload_name: NUMA Cache Latency Sweep
        meaning: NUMA node on which the benchmark memory region resides (e.g. 0, 1, 2)
      '210':
        workload_name: NUMA Cache Latency Sweep
        meaning: NUMA node on which the benchmark memory region resides (e.g. 0, 1, 2)
      '310':
        workload_name: NUMA Cache Latency Sweep
        meaning: NUMA node on which the benchmark memory region resides (e.g. 0, 1, 2)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  temp_threshold:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 103
    - 203
    - 303
    workload_specific_overrides:
      '103':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: post-run maximum allowable CPU and GPU temperature in °C (default 75°C)
      '203':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: maximum allowable CPU and GPU package temperature in °C before flagging (default 75°C)
      '303':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: post-run maximum allowable CPU and GPU temperature in °C (default 75°C)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  temperature_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 102
    - 202
    - 302
    workload_specific_overrides:
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: captures GPU package temperature telemetry (default true)
      '202':
        workload_name: NVIDIA System Validation (clocks, temperature, etc.)
        meaning: captures GPU temperature
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: captures GPU package temperature telemetry (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  throttle_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 103
    - 203
    - 303
    workload_specific_overrides:
      '103':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: configured throttling validation field; active throttling detection is not currently implemented (default
          true)
      '203':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: monitor for CPU and GPU clock throttling events (default true)
      '303':
        workload_name: CPU + System Stress (Baseline Health)
        meaning: configured throttling validation field; active throttling detection is not currently implemented (default
          true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  time:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 108
    - 208
    - 308
    workload_specific_overrides:
      '108':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '208':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '308':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  time_based:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: if set to 1, run exactly for runtime seconds regardless of data exhaustion
      '206':
        workload_name: FIO NVMe Sweep
        meaning: if set to 1, run exactly for runtime seconds regardless of data exhaustion
      '306':
        workload_name: FIO NVMe Sweep
        meaning: if set to 1, run exactly for runtime seconds regardless of data exhaustion
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  top_k:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 132
    - 232
    - 332
    workload_specific_overrides:
      '132':
        workload_name: RAG Pipeline Sweep
        meaning: 5 retrieved chunks per query
      '232':
        workload_name: RAG Pipeline Sweep
        meaning: number of document chunks retrieved from the vector DB per query (default 5)
      '332':
        workload_name: RAG Pipeline Sweep
        meaning: 5 retrieved chunks per query
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  transposeA:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 217
    - 317
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: Defined by the workload description.
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  transposeB:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 117
    - 217
    - 317
    workload_specific_overrides:
      '117':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: 'whether to transpose A and/or B before multiply. alpha, beta: scalar multipliers for the product and accumulator.
          lda / ldb / ldc: leading dimension (stride in contiguous elements) for columns of A, B, C'
      '217':
        workload_name: GEMM / cuBLAS Microbenchmark
        meaning: 'whether to transpose A and/or B before multiply. alpha, beta: scalar multipliers for the product and accumulator.
          lda / ldb / ldc: leading dimension (stride in contiguous elements) for columns of A, B, C'
      '317':
        workload_name: GEMM / rocBLAS Microbenchmark
        meaning: 'whether to transpose A and/or B before multiply. alpha, beta: scalar multipliers for the product and accumulator.
          lda / ldb / ldc: leading dimension (stride in contiguous elements) for columns of A, B, C'
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  warmup_duration_sec:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 106
    - 206
    - 306
    workload_specific_overrides:
      '106':
        workload_name: FIO NVMe Sweep
        meaning: warm-up period per job in seconds
      '206':
        workload_name: FIO NVMe Sweep
        meaning: warm-up period per job in seconds
      '306':
        workload_name: FIO NVMe Sweep
        meaning: warm-up period per job in seconds
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  warmup_duration_us:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 112
    - 212
    - 312
    workload_specific_overrides:
      '112':
        workload_name: lmbench Microbench Suite
        meaning: per-benchmark warm-up period in microseconds (default 1,000,000 µs)
      '212':
        workload_name: lmbench Microbench Suite
        meaning: per-benchmark warm-up period in microseconds (default 1,000,000 µs)
      '312':
        workload_name: lmbench Microbench Suite
        meaning: per-benchmark warm-up period in microseconds (default 1,000,000 µs)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  weight_decay:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 125
    - 225
    - 325
    workload_specific_overrides:
      '125':
        workload_name: DistilBERT NLP Training Baseline
        meaning: AdamW L2 regularization coefficient (default 0.01). each batch performs one optimizer update
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: L2 regularization coefficient applied by the optimizer (default 0.01)
      '325':
        workload_name: DistilBERT NLP Training Baseline
        meaning: AdamW L2 regularization coefficient (default 0.01). each batch performs one optimizer update
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  weights:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 122
    - 222
    - 322
    workload_specific_overrides:
      '122':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: ResNet-50 weight initialization — random or pretrained
      '222':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: ResNet-50 weight initialization — random or pretrained
      '322':
        workload_name: ResNet-50 BF16 Inference Throughput Sweep
        meaning: ResNet-50 weight initialization — random or pretrained
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  window_size:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 108
    - 208
    - 308
    workload_specific_overrides:
      '108':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '208':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
      '308':
        workload_name: iPerf3 TCP/UDP Sweep
        meaning: Defined by the workload description.
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  workload_commands:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 111
    - 211
    - 311
    workload_specific_overrides:
      '111':
        workload_name: Linux perf PMU Analysis Harness
        meaning: shell command(s) to profile (e.g. ./run_benchmark.sh), including stress-ng variants for branch predictor,
          L1/L2 cache, ALU/FPU compute, LLC, dTLB/page walker, and iTLB/instruction cache
      '211':
        workload_name: Linux perf PMU Analysis Harness
        meaning: shell command(s) to profile (e.g. ./run_benchmark.sh), including stress-ng variants for branch predictor,
          L1/L2 cache, ALU/FPU compute, LLC, dTLB/page walker, and iTLB/instruction cache
      '311':
        workload_name: Linux perf PMU Analysis Harness
        meaning: shell command(s) to profile (e.g. ./run_benchmark.sh), including stress-ng variants for branch predictor,
          L1/L2 cache, ALU/FPU compute, LLC, dTLB/page walker, and iTLB/instruction cache
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  driver_version:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 101
    - 301
    workload_specific_overrides:
      '101':
        workload_name: System Config Verification
        meaning: expected amdgpu kernel driver version (e.g. amdgpu 6.x)
      '301':
        workload_name: System Config Verification
        meaning: expected amdgpu kernel driver version (e.g. amdgpu 6.x)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  gradient_accumulation:
    description: Number of forward/backward passes accumulated per optimizer update.
    data_type: integer
    units: forward passes
    default_value: workload-specific
    valid_range: integer >= 1
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 221
    - 225
    workload_specific_overrides:
      '221':
        workload_name: ResNet‑50 FP16 Training Step (PyTorch)
        meaning: number of forward/backward passes accumulated before each optimizer step
      '225':
        workload_name: DistilBERT NLP Training Baseline
        meaning: forward passes accumulated before each optimizer update
    cli_config_key: workload-specific
    interaction_precedence: changes effective batch size and optimizer-step frequency
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  hsa_agent_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 102
    - 302
    workload_specific_overrides:
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: validates HSA agent enumeration and attributes (default true)
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: validates HSA agent enumeration and attributes (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  max_running_requests:
    description: Maximum server-side requests actively running.
    data_type: integer
    units: requests
    default_value: workload-specific
    valid_range: integer >= 1
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 230
    - 231
    workload_specific_overrides:
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: maximum server-side requests running simultaneously
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: maximum server-side requests running simultaneously
    cli_config_key: workload-specific
    interaction_precedence: server capacity; excess requests queue or are rejected
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  queue_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 102
    - 302
    workload_specific_overrides:
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: checks HSA queue capabilities for all detected agents (default true)
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: checks HSA queue capabilities for all detected agents (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  ras_check:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 102
    - 302
    workload_specific_overrides:
      '102':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: reads AMD RAS (Reliability, Availability, Serviceability) telemetry counters (default true)
      '302':
        workload_name: ROCm System Validation (clocks, temp, etc.)
        meaning: reads AMD RAS (Reliability, Availability, Serviceability) telemetry counters (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  rocm_version:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 101
    - 301
    workload_specific_overrides:
      '101':
        workload_name: System Config Verification
        meaning: expected ROCm stack version (e.g. 7.2.1)
      '301':
        workload_name: System Config Verification
        meaning: expected ROCm stack version (e.g. 7.2.1)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  schedule_policy:
    description: Request scheduling policy.
    data_type: string
    units: policy token
    default_value: workload-specific
    valid_range: supported backend policy token
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 230
    - 231
    workload_specific_overrides:
      '230':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: request scheduling algorithm — fcfs, random, etc
      '231':
        workload_name: SGLang Serving Latency Benchmark
        meaning: request scheduling algorithm — fcfs, random, etc
    cli_config_key: workload-specific
    interaction_precedence: affects queueing, fairness, and latency
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
  streaming:
    description: Shared workload parameter.
    data_type: string
    units: workload-specific
    default_value: workload-specific
    valid_range: workload-specific
    required: workload-specific
    semantic_scope: workload-specific; see workload definitions below
    workloads_using:
    - 130
    - 330
    workload_specific_overrides:
      '130':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: enable streamed responses so TTFT can be measured (default true)
      '330':
        workload_name: SGLang Prompt‑Response Benchmark
        meaning: enable streamed responses so TTFT can be measured (default true)
    cli_config_key: workload-specific
    interaction_precedence: workload-specific interaction rules
    notes_constraints: Do not assume this shared name has identical units or semantics across all workloads.
